# 更新日志：115 整理与 STRM 受控能力发布（2026-07-30）

> 历史发布记录：本文件只保留该日期的发布包、部署和受管夹具证据。它不把阶段能力
> 视为 REQ-001/REQ-002 完成，也不代表当前生产开关已开启。

## 发布信息

- 代码提交：`6808ab4`。
- 发布包：`watch-assistant-6808ab4-20260730-1242.tar.gz`。
- 部署方式：systemd，服务器 `192.168.6.236:8115`。
- 发布后 `VERSION`、健康检查和前端静态资源核对一致。

## 该次发布验证范围

- 115 整理设置支持定时开关、扫描间隔、源目录和目标目录配置。
- 支持立即整理排队、停止定时整理、计划预览、计划摘要确认、远端前置条件核对和审计。
- 目标目录扫描支持已有文件冲突比较，并限制目录和文件读取规模。
- 支持受控移动、重命名、覆盖前回收旧目标，以及空目录安全清理门禁。
- 整理完成后按开关联动 STRM 增量任务；未启用时只记录跳过事件。
- 覆盖策略支持 Remux、分辨率、杜比、文件大小和杜比/非杜比多版本判断。
- 真实低风险 115 fixture 已完成只读扫描、计划确认、临时目录创建、移动、重命名、恢复和回收验证，共 6 个写回执；原 fixture 状态已复核恢复。

## 验证结果

- Python unit：525 passed，3 skipped。
- Python integration：全部测试文件通过。
- Python contracts：167 passed。
- Ruff：通过。
- 前端 Vitest：106 passed；Playwright：149 passed；Vite production build：通过。
- 生产健康接口：`status=ok`；组织写入契约已验证；永久删除和 STRM 清理仍关闭。

## 当前边界

- 永久删除保持关闭，只允许可核对的回收语义。
- STRM 清理保持关闭；STRM 全量、增量和播放能力仍受独立门禁控制。
- 媒体技术信息目前只保留已解析标签，尚未接入真实 `ffprobe` 远端探测。
- AI 辅助识别尚未接入 AI 服务，配置未完成时不会伪造调用。
- 任何真实 115 写操作都必须继续使用已持久化计划、人工确认、范围校验和不确定结果核对。
