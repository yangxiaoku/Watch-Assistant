# 115 网盘自动签到 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Watch-Assistant 每天按配置时间(默认 00:05)自动调用 115 积分签到接口,成功/失败都通过现有通知管线推送,并在设置页显示开关与最近签到结果。

**Architecture:** 新增 `P115CheckInService`(封装 p115client `user_points_sign_post`)+ `P115CheckInScheduler`(每日调度,用 115 的 `is_sign_today` 做同日幂等),接线在 app.py lifespan;开关默认关闭,用户在设置页开启。事件经 `emit_event` → 现有通知桥接推送。

**Tech Stack:** Python 3.12, FastAPI, p115client(0.0.9.6.5.1), SQLAlchemy 2.0 async, Vue 3 + TS。

## Global Constraints

- 签到接口:`POST https://proapi.115.com/android/2.0/user/points_sign`(p115client `user_points_sign_post(app='android')`);只读:`user_points_sign(app='android')`。
- 不要用 web 端点 `webapi.115.com/user/add_hd` / `user/checkin`(已弃用,返回"服务器开小差了")。
- `P115_CHECK_IN_ENABLED` 默认 `false`;开关关闭时零网络调用。
- 失败不自动重试(次日自然再签);同一天只签到一次(以 115 `is_sign_today` 为准)。
- 事件码 `p115.checkin.succeeded` / `p115.checkin.failed` 必须注册进 event_catalog(契约测试覆盖)。
- 不写 credentials/cookie 到代码、日志、测试、输出;DTO repr 不含 cookie。

---

### Task 1: 配置字段 + 校验

**Files:**
- Modify: `src/watch_assistant/config.py`
- Test: `tests/unit/test_config.py`

**Interfaces:**
- Produces: `Settings.p115_check_in_enabled: bool`(env `P115_CHECK_IN_ENABLED`,默认 False);`Settings.p115_check_in_time: str`(env `P115_CHECK_IN_TIME`,默认 `"00:05"`,校验 `HH:MM`)。

- [ ] **Step 1: 写失败测试**

在 `tests/unit/test_config.py` 追加:

```python
def test_p115_checkin_config_defaults_and_validation():
    from watch_assistant.config import Settings
    from pydantic import ValidationError
    import pytest as _pytest

    settings = Settings(
        DATABASE_URL="sqlite:///x.db",
        ENCRYPTION_KEY="0" * 44,
        TMDB_API_KEY="k",
        SCRIPT_TOKEN_HASH="h",
        PANSOU_BASE_URL="http://p",
    )
    assert settings.p115_check_in_enabled is False
    assert settings.p115_check_in_time == "00:05"

    settings = Settings(
        DATABASE_URL="sqlite:///x.db", ENCRYPTION_KEY="0" * 44,
        TMDB_API_KEY="k", SCRIPT_TOKEN_HASH="h", PANSOU_BASE_URL="http://p",
        P115_CHECK_IN_ENABLED="true", P115_CHECK_IN_TIME="09:30",
    )
    assert settings.p115_check_in_enabled is True
    assert settings.p115_check_in_time == "09:30"

    with _pytest.raises(ValidationError):
        Settings(DATABASE_URL="sqlite:///x.db", ENCRYPTION_KEY="0" * 44,
                 TMDB_API_KEY="k", SCRIPT_TOKEN_HASH="h", PANSOU_BASE_URL="http://p",
                 P115_CHECK_IN_TIME="25:00")
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_config.py -q`
Expected: FAIL(`Settings` 没有 `p115_check_in_enabled` 属性)

- [ ] **Step 3: 实现**

在 `config.py` 的 `Settings` 类加:

```python
    p115_check_in_enabled: bool = Field(
        default=False, validation_alias="P115_CHECK_IN_ENABLED"
    )
    # 每日签到时刻 HH:MM(本地时区),默认 00:05 跨天最早签到。
    p115_check_in_time: str = Field(
        default="00:05",
        min_length=5,
        max_length=5,
        pattern=r"^([01]\d|2[0-3]):[0-5]\d$",
        validation_alias="P115_CHECK_IN_TIME",
    )
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_config.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/watch_assistant/config.py tests/unit/test_config.py
git commit -m "feat(checkin): config flags for 115 auto check-in"
```

---

### Task 2: 事件目录注册 + 通知严重度

**Files:**
- Modify: `src/watch_assistant/services/event_catalog.py`
- Modify: `src/watch_assistant/services/notifications.py`
- Test: `tests/contracts/test_event_catalog.py`(自动覆盖)

**Interfaces:**
- Produces: 事件 `p115.checkin.succeeded`(LogCategory.BUSINESS,字段 points/continuous_day)、`p115.checkin.failed`(字段 error_code)。通知严重度:succeeded=INFO, failed=WARNING。

- [ ] **Step 1: 注册事件**

在 `event_catalog.py` 加两个事件(参照现有 `p115.*` 事件):

```python
    "p115.checkin.succeeded": _event(
        "p115.checkin.succeeded",
        LogCategory.BUSINESS,
        "115 签到成功",
        "115 每日签到成功,获得 {points} 积分,连续 {continuous_day} 天",
    ),
    "p115.checkin.failed": _event(
        "p115.checkin.failed",
        LogCategory.BUSINESS,
        "115 签到失败",
        "115 每日签到失败,错误码:{error_code}",
    ),
```

- [ ] **Step 2: 通知严重度映射**

在 `notifications.py` 的严重度函数里补 `p115.checkin.failed` → WARNING(参照 `inspection.batch_failed` 的处理;`p115.checkin.succeeded` 走默认 INFO)。

- [ ] **Step 3: 跑契约测试**

Run: `pytest tests/contracts/test_event_catalog.py -q`
Expected: PASS(新增事件被 `_literal_event_calls` 覆盖后仍通过)

- [ ] **Step 4: Commit**

```bash
git add src/watch_assistant/services/event_catalog.py src/watch_assistant/services/notifications.py
git commit -m "feat(checkin): register check-in events and notification severity"
```

---

### Task 3: P115CheckInService

**Files:**
- Create: `src/watch_assistant/services/p115_checkin.py`
- Test: `tests/unit/test_p115_checkin.py`

**Interfaces:**
- Consumes: cookie provider(`load() -> str | None`)。
- Produces:
  - `@dataclass(frozen=True) class CheckInResult: points: str; continuous_day: int`
  - `class CheckInUnavailable(ValueError): code: str`
  - `class P115CheckInService: __init__(self, cookie_provider, *, event_logger=None, client_factory=None)`
  - `async def check_in(self) -> CheckInResult`
  - `async def status(self) -> dict`(返回 `{"is_sign_today": bool, "continuous_day": int, "points_num": str}`)

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/test_p115_checkin.py`:

```python
import pytest

from watch_assistant.services.p115_checkin import (
    CheckInUnavailable,
    P115CheckInService,
)


class _FakeCookieProvider:
    def __init__(self, cookie: str | None):
        self._cookie = cookie
    def load(self) -> str | None:
        return self._cookie


class _FakeClient:
    def __init__(self, sign_post=None, sign=None):
        self._sign_post = sign_post or {"state": True, "data": {"points_num": "7", "continuous_day": 7}}
        self._sign = sign or {"state": True, "data": {"is_sign_today": 1, "continuous_day": 7}}
    async def user_points_sign_post(self, app="android", async_=True):
        return self._sign_post
    async def user_points_sign(self, app="android", async_=True):
        return self._sign


@pytest.mark.asyncio
async def test_check_in_returns_points_and_streak():
    service = P115CheckInService(_FakeCookieProvider("cookie"), client_factory=lambda c: _FakeClient())
    result = await service.check_in()
    assert result.points == "7"
    assert result.continuous_day == 7


@pytest.mark.asyncio
async def test_check_in_missing_cookie_raises_credential_unavailable():
    service = P115CheckInService(_FakeCookieProvider(None), client_factory=lambda c: _FakeClient())
    with pytest.raises(CheckInUnavailable) as exc:
        await service.check_in()
    assert exc.value.code == "credential_unavailable"


@pytest.mark.asyncio
async def test_check_in_rejected_state_raises():
    client = _FakeClient(sign_post={"state": False, "error": "rejected"})
    service = P115CheckInService(_FakeCookieProvider("cookie"), client_factory=lambda c: client)
    with pytest.raises(CheckInUnavailable) as exc:
        await service.check_in()
    assert exc.value.code == "checkin_rejected"


@pytest.mark.asyncio
async def test_status_reports_today_signed():
    service = P115CheckInService(_FakeCookieProvider("cookie"), client_factory=lambda c: _FakeClient())
    status = await service.status()
    assert status["is_sign_today"] is True
    assert status["continuous_day"] == 7
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_p115_checkin.py -q`
Expected: FAIL(ModuleNotFoundError)

- [ ] **Step 3: 实现服务**

创建 `src/watch_assistant/services/p115_checkin.py`:

```python
"""115 每日积分签到(proapi 积分系统),fail-closed。"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Awaitable, Callable


@dataclass(frozen=True)
class CheckInResult:
    points: str
    continuous_day: int


class CheckInUnavailable(ValueError):
    def __init__(self, code: str) -> None:
        self.code = code
        super().__init__(code)


ClientFactory = Callable[[str], Any]


class P115CheckInService:
    def __init__(
        self,
        cookie_provider,
        *,
        event_logger=None,
        client_factory: ClientFactory | None = None,
    ) -> None:
        self._cookie_provider = cookie_provider
        self._event_logger = event_logger
        self._client_factory = client_factory

    def _client(self):
        cookie = self._cookie_provider.load()
        if not cookie:
            raise CheckInUnavailable("credential_unavailable")
        if self._client_factory is not None:
            return self._client_factory(cookie)
        from p115client import P115Client

        return P115Client(cookie, console_qrcode=False)

    async def status(self) -> dict:
        try:
            client = self._client()
            response = await client.user_points_sign(app="android", async_=True)
        except CheckInUnavailable:
            raise
        except Exception as exc:  # noqa: BLE001 - remote detail stays opaque
            raise CheckInUnavailable("checkin_unavailable") from exc
        data = response.get("data", {}) if isinstance(response, dict) else {}
        return {
            "is_sign_today": bool(data.get("is_sign_today")),
            "continuous_day": int(data.get("continuous_day") or 0),
            "points_num": str(data.get("points_num") or ""),
        }

    async def check_in(self) -> CheckInResult:
        try:
            client = self._client()
            response = await client.user_points_sign_post(app="android", async_=True)
        except CheckInUnavailable:
            raise
        except Exception as exc:  # noqa: BLE001 - remote detail stays opaque
            raise CheckInUnavailable("checkin_unavailable") from exc
        if not isinstance(response, dict) or response.get("state") is not True:
            raise CheckInUnavailable("checkin_rejected")
        data = response.get("data", {}) or {}
        return CheckInResult(
            points=str(data.get("points_num") or ""),
            continuous_day=int(data.get("continuous_day") or 0),
        )
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_p115_checkin.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/watch_assistant/services/p115_checkin.py tests/unit/test_p115_checkin.py
git commit -m "feat(checkin): P115CheckInService wrapping proapi points_sign"
```

---

### Task 4: P115CheckInScheduler(每日触发 + 同日幂等)

**Files:**
- Create: `src/watch_assistant/services/p115_checkin_scheduler.py`
- Test: `tests/unit/test_p115_checkin_scheduler.py`

**Interfaces:**
- Consumes:`P115CheckInService.check_in()/status()`、`CheckInUnavailable`;事件码。
- Produces:
  - `class P115CheckInScheduler: __init__(self, service, *, event_logger=None, timezone="Asia/Hong_Kong", check_in_time="00:05")`
  - `async def run_due_once(self) -> bool`(到点且今日未签→签到,返回是否执行)
  - `async def run_forever(self, stop_event, *, interval_seconds=300)`

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/test_p115_checkin_scheduler.py`:

```python
from datetime import datetime, timedelta

import pytest

from watch_assistant.services.p115_checkin import CheckInResult, CheckInUnavailable
from watch_assistant.services.p115_checkin_scheduler import P115CheckInScheduler


class _FakeService:
    def __init__(self, status=None, result=None, fail=False):
        self._status = status or {"is_sign_today": False, "continuous_day": 0, "points_num": ""}
        self._result = result or CheckInResult(points="7", continuous_day=8)
        self._fail = fail
        self.check_in_calls = 0
        self.status_calls = 0
    async def status(self):
        self.status_calls += 1
        return self._status
    async def check_in(self):
        self.check_in_calls += 1
        if self._fail:
            raise CheckInUnavailable("checkin_rejected")
        return self._result


class _Clock:
    def __init__(self, now):
        self.now = now
    def __call__(self):
        return self.now


async def _run_due(scheduler):
    return await scheduler.run_due_once()


@pytest.mark.asyncio
async def test_scheduler_skips_before_target_time():
    service = _FakeService()
    scheduler = P115CheckInScheduler(
        service, timezone="Asia/Hong_Kong", check_in_time="00:05", _clock=_Clock(datetime(2026, 8, 13, 0, 0))
    )
    assert await _run_due(scheduler) is False
    assert service.check_in_calls == 0


@pytest.mark.asyncio
async def test_scheduler_fires_after_target_when_not_signed():
    service = _FakeService()
    scheduler = P115CheckInScheduler(
        service, timezone="Asia/Hong_Kong", check_in_time="00:05", _clock=_Clock(datetime(2026, 8, 13, 0, 6))
    )
    assert await _run_due(scheduler) is True
    assert service.check_in_calls == 1


@pytest.mark.asyncio
async def test_scheduler_skips_when_already_signed_today():
    service = _FakeService(status={"is_sign_today": True, "continuous_day": 7, "points_num": "7"})
    scheduler = P115CheckInScheduler(
        service, timezone="Asia/Hong_Kong", check_in_time="00:05", _clock=_Clock(datetime(2026, 8, 13, 0, 6))
    )
    assert await _run_due(scheduler) is False
    assert service.check_in_calls == 0


@pytest.mark.asyncio
async def test_scheduler_records_failure_event_without_raising():
    service = _FakeService(fail=True)
    scheduler = P115CheckInScheduler(
        service, timezone="Asia/Hong_Kong", check_in_time="00:05", _clock=_Clock(datetime(2026, 8, 13, 0, 6))
    )
    assert await _run_due(scheduler) is False
    assert service.check_in_calls == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/unit/test_p115_checkin_scheduler.py -q`
Expected: FAIL(ModuleNotFoundError)

- [ ] **Step 3: 实现调度器**

创建 `src/watch_assistant/services/p115_checkin_scheduler.py`:

```python
"""每日 115 签到调度器:到配置时刻且今日未签时执行一次,失败不重试。"""

from __future__ import annotations

import asyncio
from datetime import datetime
from typing import Callable
from zoneinfo import ZoneInfo

from watch_assistant.services.observability import EventLogger, emit_event
from watch_assistant.services.p115_checkin import CheckInUnavailable, P115CheckInService


class P115CheckInScheduler:
    def __init__(
        self,
        service: P115CheckInService,
        *,
        event_logger: EventLogger | None = None,
        timezone: str = "Asia/Hong_Kong",
        check_in_time: str = "00:05",
        _clock: Callable[[], datetime] | None = None,
    ) -> None:
        self._service = service
        self._event_logger = event_logger
        self._timezone = timezone
        self._check_in_time = check_in_time
        self._clock = _clock
        self._run_lock = asyncio.Lock()

    def _now(self) -> datetime:
        if self._clock is not None:
            return self._clock()
        return datetime.now(ZoneInfo(self._timezone))

    def _due(self, now: datetime) -> bool:
        hour, minute = (int(p) for p in self._check_in_time.split(":"))
        return (now.hour, now.minute) >= (hour, minute)

    async def run_due_once(self) -> bool:
        async with self._run_lock:
            now = self._now()
            if not self._due(now):
                return False
            try:
                status = await self._service.status()
                if status["is_sign_today"]:
                    return False
            except CheckInUnavailable:
                pass  # 状态读失败仍尝试签到;115 幂等保证不重复奖励
            try:
                result = await self._service.check_in()
            except CheckInUnavailable as exc:
                await emit_event(
                    self._event_logger,
                    "p115.checkin.failed",
                    level=_WARNING,
                    fields={"error_code": exc.code},
                )
                return False
            await emit_event(
                self._event_logger,
                "p115.checkin.succeeded",
                fields={
                    "points": result.points,
                    "continuous_day": result.continuous_day,
                },
            )
            return True

    async def run_forever(self, stop_event: asyncio.Event, *, interval_seconds: float = 300) -> None:
        while not stop_event.is_set():
            try:
                await asyncio.wait_for(stop_event.wait(), timeout=interval_seconds)
            except TimeoutError:
                pass
            if stop_event.is_set():
                break
            try:
                await self.run_due_once()
            except asyncio.CancelledError:
                raise
            except Exception:  # noqa: BLE001 - scheduler loop must stay alive
                continue


from watch_assistant.schemas import LoggingLevel

_WARNING = LoggingLevel.WARNING
```

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/unit/test_p115_checkin_scheduler.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/watch_assistant/services/p115_checkin_scheduler.py tests/unit/test_p115_checkin_scheduler.py
git commit -m "feat(checkin): daily scheduler with is_sign_today idempotency"
```

---

### Task 5: 设置持久化 + API(开关 + 状态)

**Files:**
- Modify: `src/watch_assistant/models.py`(ApplicationSettings 加列)
- Modify: `src/watch_assistant/migrations.py`(迁移 076)
- Modify: `src/watch_assistant/services/settings.py`
- Modify: `src/watch_assistant/schemas.py`
- Create: `src/watch_assistant/api/p115_checkin.py`
- Modify: `src/watch_assistant/app.py`(挂路由)
- Test: `tests/unit/test_migrations.py`、`tests/integration/test_settings_api.py`

**Interfaces:**
- Produces: 设置 `{enabled: bool, check_in_time: str}`;API `GET /api/v1/settings/p115-checkin`、`PATCH /api/v1/settings/p115-checkin`、`GET /api/v1/p115-checkin/status`(只读签到状态)。
- 有效 enabled = 用户设置(若显式设置)否则 env 默认。

- [ ] **Step 1: 迁移 + 模型**

在 migrations.py 注册迁移 `076_p115_checkin_settings`,给 application_settings 加 `p115_checkin_settings_json TEXT NOT NULL DEFAULT '{}'`(参照 `_add_organization_settings_column`)。models.py 加同名列。在 `test_migrations.py` 加迁移测试(旧库跑 076 后列存在)。

- [ ] **Step 2: 设置服务 get/set**

在 settings.py 加:

```python
def _p115_checkin_dict(settings: ApplicationSettings) -> dict:
    raw = settings.p115_checkin_settings_json or "{}"
    try:
        value = json.loads(raw)
    except ValueError:
        value = {}
    return value if isinstance(value, dict) else {}

async def get_p115_checkin(self) -> P115CheckInSettingsResponse:
    async with self._session_factory() as session:
        settings = await session.get(ApplicationSettings, "default")
        value = _p115_checkin_dict(settings) if settings else {}
    return P115CheckInSettingsResponse(
        enabled=bool(value.get("enabled", False)),
        check_in_time=str(value.get("check_in_time") or "00:05"),
    )

async def set_p115_checkin(self, enabled: bool, check_in_time: str) -> None:
    async with self._session_factory() as session:
        settings = await session.get(ApplicationSettings, "default")
        if settings is None:
            settings = ApplicationSettings(id="default")
            session.add(settings)
        value = _p115_checkin_dict(settings)
        value["enabled"] = enabled
        value["check_in_time"] = check_in_time
        settings.p115_checkin_settings_json = json.dumps(value, ensure_ascii=False)
        settings.revision += 1
        await session.commit()
```

- [ ] **Step 3: schemas**

在 schemas.py 加 `P115CheckInSettingsResponse`(enabled: bool, check_in_time: str)、`P115CheckInSettingsPatch`(enabled: bool, check_in_time: str = "00:05")、`P115CheckInStatusResponse`(enabled, is_sign_today, continuous_day, points_num)。

- [ ] **Step 4: API 路由**

创建 `src/watch_assistant/api/p115_checkin.py`,三个端点(鉴权 `require_api_auth`):
- `GET /settings/p115-checkin` → 读设置。
- `PATCH /settings/p115-checkin` → 写设置(校验时间格式)。
- `GET /p115-checkin/status` → 调 `P115CheckInService.status()`(只读),失败返回 `{"enabled":..., "is_sign_today": false, "error_code": "..."}`(不 500)。

- [ ] **Step 5: app.py 挂路由 + 服务注入**

在 create_app 里 `application.state.p115_checkin_service = P115CheckInService(cookie_provider, event_logger=settings_service)`,include router。迁移测试跑通。

- [ ] **Step 6: 测试**

`tests/unit/test_migrations.py` 新迁移测试通过;`tests/integration/test_settings_api.py` 加 PATCH/GET 往返测试。

- [ ] **Step 7: Commit**

```bash
git add src/watch_assistant/models.py src/watch_assistant/migrations.py src/watch_assistant/services/settings.py src/watch_assistant/schemas.py src/watch_assistant/api/p115_checkin.py src/watch_assistant/app.py tests/
git commit -m "feat(checkin): settings persistence + API (toggle + read-only status)"
```

---

### Task 6: app.py 调度器接线

**Files:**
- Modify: `src/watch_assistant/app.py`
- Test: `tests/integration/test_app.py`

**Interfaces:**
- Consumes: `P115CheckInScheduler`、`Settings.p115_check_in_time`、`get_p115_checkin()`。
- Produces: lifespan 启动时若有效 enabled 为 True 且 p115 就绪,创建 `P115CheckInScheduler` 并 `run_forever`。

- [ ] **Step 1: 写接线**

在 lifespan(参照 subscription_scheduler 接线,~1334 行附近)加:

```python
        p115_checkin_settings = await application.state.settings_service.get_p115_checkin()
        # 有效开关 = 用户设置 OR 部署 env 默认(P115_CHECK_IN_ENABLED)
        effective_enabled = p115_checkin_settings.enabled or settings.p115_check_in_enabled
        if effective_enabled and getattr(application.state, "p115_ready", False):
            application.state.p115_checkin_scheduler = P115CheckInScheduler(
                application.state.p115_checkin_service,
                event_logger=application.state.settings_service,
                timezone=settings.cache_warm_timezone,
                check_in_time=p115_checkin_settings.check_in_time,
            )
            p115_checkin_stop = asyncio.Event()
            p115_checkin_task = asyncio.create_task(
                application.state.p115_checkin_scheduler.run_forever(p115_checkin_stop),
                name="watch-assistant-p115-checkin-scheduler",
            )
```

- [ ] **Step 2: 测试**

在 `tests/integration/test_app.py` 或 `test_settings_api.py` 加:设置 enabled=true 后,断言 `application.state.p115_checkin_scheduler` 存在;enabled=false 时不存在。

- [ ] **Step 3: 跑 app 测试 + Commit**

Run: `pytest tests/integration/test_app.py -q`(预期 PASS)
Commit: `git commit -m "feat(checkin): wire scheduler into app lifespan"`

---

### Task 7: 前端设置页开关 + 状态

**Files:**
- Modify: `frontend/src/api.ts`、`frontend/src/types.ts`
- Modify: `frontend/src/views/SettingsView.vue`
- Test: `frontend/tests/SettingsView.spec.ts`

**Interfaces:**
- Consumes: `GET/PATCH /api/v1/settings/p115-checkin`、`GET /api/v1/p115-checkin/status`。
- Produces: api.ts 方法 `p115CheckinSettings()` / `updateP115CheckinSettings(patch)` / `p115CheckinStatus()`。

- [ ] **Step 1: api.ts + types.ts**

加三个方法 + 类型 `P115CheckInSettingsResponse`/`P115CheckInStatusResponse`。

- [ ] **Step 2: SettingsView 加区块**

在 SettingsView.vue 加"115 自动签到"区块:开关(默认关,保存走 PATCH)、显示最近签到状态(今日是否已签、连续天数、积分)。加载时 GET settings + status。

- [ ] **Step 3: 前端测试 + build**

在 `frontend/tests/SettingsView.spec.ts` 加测试:开关切换调用 PATCH;状态区显示已签/未签。跑 `npm test -- --run tests/SettingsView.spec.ts` 通过 + `npm run build` 通过。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/api.ts frontend/src/types.ts frontend/src/views/SettingsView.vue frontend/tests/SettingsView.spec.ts
git commit -m "feat(checkin): settings UI toggle + status display"
```

---

### Task 8: 全量验证 + 部署

**Files:**
- None(验证 + 部署)

- [ ] **Step 1: verify.sh**

Run: `./scripts/verify.sh`
Expected: exit 0(unit/integration/contract/ruff/frontend 全绿)

- [ ] **Step 2: commit + push + build + deploy**

```bash
git push origin codex/publish-main
bash scripts/build_release.sh <full-sha>
bash scripts/verify_release_artifact.sh <artifact> <sha> /tmp/release-verify
scp artifact root@192.168.6.236:/tmp/ && ssh deploy
```

- [ ] **Step 3: 线上验证**

- 登录 → `GET /api/v1/settings/p115-checkin`(enabled=false 默认)。
- `PATCH` enabled=true → 重启服务 → journal 无错误。
- `GET /api/v1/p115-checkin/status` → 返回 is_sign_today(今日已签为 true)。
- 前端 `/settings` 显示开关 + 状态。
- 若当天未签,观察 0:05 自动签到(或手动触发 `run_due_once` 验证事件/通知)。
