# 需求阻断记录

更新时间：2026-07-31

本记录用于按需求优先级推进时记录无法安全继续的事项。阻断某项需求后，继续处理不依赖该阻断的需求；未完成的能力不得描述为已上线。

## 已完成的可执行范围

- REQ-001：完成 P115 只读目录契约、分页断点索引、媒体文件名解析、TMDB 匹配、分类命名和整理计划预览；本轮在受管一次性夹具上完成双文件上传、移动、重命名、恢复和精确回收，证据见 `evidence/req-001-002-live-companion-20260730.json`；真实业务整理和生产写入仍关闭。
- REQ-001：真实 C03 低风险测试目录写入验收已通过：10 次写操作逐次核对通过，完成建目录、移动、重命名、隔离/恢复组合和回收，15 次列表观察、28 页读取，cleanup 为 `complete`；未触碰生产目录，业务整理器仍未开启。生产目标根目录递归只读基线已完成：54 页、12 个目录、11 个子目录、43 个文件、54 个唯一对象身份，证据见 `evidence/req-001-production-inventory-20260729.json`。
- REQ-001 本轮补充：新增受控 `LiveP115OrganizationTransport`，固定版本客户端、调用级超时、scope/计划摘要/一次性授权门禁和 receipt-before-read-after-write 已通过离线契约及 C03 测试目录真实验收；本次进一步完成应用设置、只读扫描、计划确认、操作排队、真实 worker 移动/标准化重命名、恢复和测试树回收，证据见 `evidence/req-001-application-worker-live-20260730.json`。生产业务整理仍未自动开启。
- REQ-004：完成结构化事件目录、中文字段、请求/关联 ID、状态/错误码、脱敏 JSONL、参数化日志查询、JSONL/CSV 导出和 `audit_records` 持久审计基础设施；隔离、整理操作及 STRM 清理开始/结束已接入同一事务审计；登录、登出、CSRF、限流和采样 Agent Token 认证事件已接入持久审计，推送任务生命周期日志和站内通知第一阶段已覆盖。本轮新增 Agent Token 创建、暂停、恢复、撤销、请求认证和 Scope 拒绝的结构化/持久审计，并修正整理操作审计不应伪装成 `settings.changed` 的事件目录错误，专项测试通过。外部通知渠道、完整业务事件矩阵和详情时间线仍未完成。
- REQ-003：完成 `watchctl` 只读 CLI 阶段：系统状态/能力、Schema 发现、doctor、任务/工作流/通知/备份/部署诊断、媒体库、媒体条目、审计、整理计划列表/详情和 STRM capability/manifest status 查询，统一 JSON/JSONL envelope 与退出码；本轮新增 `organize apply` 的 digest/confirm/自动幂等键、`task wait` 轮询、`task retry`、queued 任务取消、Webhook 查询/测试、整理计划生成、受保护的 STRM 全量/增量命令、清理计划和 digest/confirm 执行命令，以及只读 `strm verify`（缺失、孤儿、路径和内容校验）；专项 CLI/Agent 契约回归通过。Web 高风险批准、生产播放入口、跨平台端到端验收仍未完成，生产 STRM 继续保持门控。证据见 `REQ-003-agent-cli/evidence/2026-07-31-strm-verify-cli.md`。
- REQ-019：完成 Webhook 阶段实现：HTTPS 公网/DNS 私网拦截、端点独立加密 Secret、版本化事件 envelope、HMAC、持久投递记录、指数重试、死信状态和管理员手动死信重试；真实接收端篡改/重放/重启恢复验收和完整事件矩阵仍阻断。
- REQ-020：完成 MCP 本地工具阶段，并在 `192.168.6.236` 当前 release 上完成受控远端 HTTP loopback Agent/MCP 验收：Agent Token 创建、身份认证、`initialize`、`watch://libraries` 范围过滤、缺少整理 Scope 拒绝、无效 Token 和撤销后的 401 均通过；038 迁移已应用并保留旧审计表。远程 HTTPS、重启恢复和完整兼容性验收仍阻断。
- REQ-021：完成 PWA 设备基础阶段：设备订阅加密存储、按会话隔离查询、撤销和订阅字段校验；应用壳已增加版本化缓存失效、只读 JSON/no-store 门禁、离线写保护和本站 Push 深链处理；Push 实际投递、认证恢复、弱网幂等和 Android/iOS/桌面验收仍阻断。
- REQ-007：新增默认关闭的订阅到期调度器，按 `next_check_at` 检查可调度订阅，跳过暂停/取消/完成状态，单项失败隔离；调度只执行搜索检查，不创建 115 任务，新增配置 `SUBSCRIPTION_SCHEDULER_ENABLED=false` 和离线回归。本轮完成 SUB-003 第一阶段资源观察账本、canonical key 去重、首次发现结果和观察列表 API，证据见 `REQ-007-subscriptions-auto-follow/evidence/2026-07-29-resource-observation-dedup.md`。自动推送、日历和真实上游验收仍阻断。
- REQ-005：完成部分结果与旧缓存合并、正/负/部分缓存 TTL 和缓存类型标记；真实 PanSou 兼容验收已获得结构正常的结果（中文查询返回 0 条，`Inception` 返回 693 条，其中 692 条磁力，来源为 2 个插件），非法 UTF-8 响应已做替换解码并通过回归测试；95% 脱敏对照召回率和补充关键词/软评分规则仍未完成。
- REQ-006：完成 10 分钟超时负缓存、API/前端强制重试绕过缓存、已有 torrent 只读复用、结果来源标记和 unsupported 统计修正；本轮补齐 qB `has_metadata` 完成信号识别、30 秒快速/可选 120 秒最终等待配置，并通过 60 项 qB/配置回归。远端 qB readiness 正常，6 个受控 torrent 全部 verified 且清理完成；当前 PanSou 抽取的 5 个样本仍均为 `metadata_timeout`，PanSou 样本基线继续阻断，证据见 `evidence/req-005-006-controlled-qb-20260729.json`。
- REQ-002：已接通整理完成后的持久 dirty outbox、租约 worker 和保守增量对账；本轮在受管视频夹具上通过正式 API 的范围验证、完整扫描、STRM 全量生成、改名后的增量对账、恢复和清理验收，证据见 `evidence/req-002-strm-live-20260730.json`。生产 STRM 清单、播放、元数据联动仍未开启。
- REQ-017：完成 SQLite 在线一致备份、非敏感清单、SHA-256 摘要和本地保留策略（第一阶段）；本轮补齐备份创建失败、成功和恢复预览的结构化事件（相关回归 `161 passed`）。恢复预览之外的实际覆盖恢复、回滚、完整兼容性校验和配置导入导出尚未开启。
- REQ-015：完成单条磁力/115 分享严格校验、媒体匹配、预览确认、加密资源保存和 canonical 去重（第一阶段）；内容检测、质量策略、115 分享实时可用性和批量导入尚未接入。
- REQ-024：完成组件清单、Python/SQLite/p115client 版本摘要、数据库完整性与迁移状态、TMDB/PanSou/qB/115 脱敏能力状态，以及保守的 `write_safe` 只读诊断接口（第一阶段）；升级元数据签名校验、升级前预检、维护模式排空、迁移预演/检查点回退、升级后验证、CLI 和诊断包尚未接入。
- REQ-014：完成文件名字幕格式识别、简繁中文/英文/其他语言标记、forced/SDH 标记、视频季集保守配对和规范命名预览（第一阶段）；内封字幕轨探测、字幕正文安全抽样、STRM 增量同步、视频移动联动和外部字幕源尚未接入。
- REQ-029：完成独立季度摘要字段、TMDB 季度详情解析、语言回退、SQLite 独立缓存（迁移 014）、认证 API、前端季度资料展示和竞态保护（第一阶段）；季度/集级库存完整度、订阅日历、REQ-025 动漫映射、真实多季样本和移动端视觉验收尚未完成。
- REQ-028：完成列表摘要即时首屏、独立影视资料请求、资源区域独立加载/失败/重试、季度切换局部更新、独立资源搜索任务/快照 API、SQLite 持久化与重启后恢复、脱敏详情性能遥测，以及客户端取消等待/30 秒超时后服务端任务继续执行；真实浏览器桌面/360px 移动端验收已通过（149 项），资源分页与目录失败恢复均已覆盖，完整中文错误目录联动已接入。后端集成命令仍有外部服务等待，未作为本次发布的通过依据。
- REQ-027：完成前端集中中文错误目录、已知错误的结果/建议/可重试动作、未知 API detail 安全兜底、任务错误码脱敏、主要入口英文文案清理和静态英文泄漏回归测试；后端统一结构化错误响应和主要错误码目录已完成，本轮将搜索、通知、质量配置和订阅 API 的受控异常改为白名单错误码，避免原始异常字符串进入用户响应。全部任务/通知动作、其余表单、PWA/CLI/MCP 覆盖仍未完成。
- REQ-009：完成只读任务中心入口、workflow 状态筛选、服务端详情读取、阶段时间线、中文状态和 correlation ID 展示；本轮补齐详情页检测/推送创建或复用 workflow、子任务 `workflow_id` 传递、push 阶段进行中状态，以及 worker 终态和重试状态回写（第一阶段）；整理 operation 现在支持受保护 API 关联 workflow，并同步排队、claim、成功、失败、不确定、取消和重试状态；推送任务新增 queued 未 claim 的原子取消和稳定错误映射，workflow 可取消未开始阶段但保留运行中/uncertain 子任务。STRM 子任务、运行中子任务的本地可中止请求、可靠通知和跨任务关联尚未完成。
- REQ-012：完成站内通知中心前端、未读数量和筛选、单条/全部已读、通知偏好开关、任务/设置跳转，以及 API 和组件离线测试；本轮补齐默认 23:00-08:00 静默时段、IANA 时区、错误/安全事件突破静默、偏好迁移和前端控件。外部渠道、渠道健康、重试/死信和完整事件矩阵仍未完成。
- REQ-025：完成无外部依赖的动漫集序映射领域模型、标准季集/绝对集数解析、确认季度偏移、显式映射优先、冲突/越界人工复核和特别内容分类（第一阶段）；库存联动、外部元数据来源、作品身份持久化和人工锁定 API 尚未完成。
- REQ-026：完成无外部读取依赖的技术探测结果模型、L1/L2 状态、稳定原因码，以及文件名声明与实测字段差异判断（第一阶段）；按用户要求本轮不纳入 115 直链、Range、媒体服务器和真实媒体样本验收。
- REQ-011：完成无外部依赖的季度集覆盖矩阵规则和 4 项离线回归测试：区分已拥有、多版本、缺失、未播出、特别篇、无法识别，并在库存不完整时将未匹配项保持为未知；TMDB 季集基线、115 库存索引、自动补集和订阅联动尚未完成。
- REQ-027：新增后端统一 `error` 响应对象、中文标题/结果/建议、可重试动作、字段错误、请求 ID/关联 ID，以及安全兜底；前端错误目录已接入主要页面和任务状态，未知 detail 不向用户暴露，目录失败 mock 已迁移到结构化错误契约；本轮补齐主页、搜索、媒体库、收藏、整理和设置页的非技术英文标签扫描，并收紧搜索、通知、质量配置和订阅 API 的受控异常码。仍有其余 API 分支、PWA/CLI/MCP 覆盖待完成。
- REQ-017：新增恢复预览接口，校验备份 SHA-256、SQLite integrity_check、迁移差异和需重新配置的 Secret 标识，并以只读方式返回可恢复/不兼容/无效状态；数据库覆盖恢复、停机排空、恢复前快照、回滚和恢复后校验仍未完成。
- REQ-018：已完成只读健康规则层和 5 项离线回归：可识别孤儿清单、失效 STRM、缺字幕、命名不规范和 uncertain 任务，输出中文原因/影响/建议、严重度、模块分数、总分和趋势；受管 STRM 仅生成可逆计划预览，非受管文件需确认，库存不完整时禁止库存依赖计划。REQ-010 生产库存、REQ-002 直链抽样、REQ-014 内封字幕和 REQ-012 通知联动仍未接入。
- REQ-016：已完成只读存储治理基础：容量与分类统计、完整快照下的精确重复候选、大文件提示、受保护候选阻断，以及不完整扫描禁止生成清理计划；模块不访问文件系统，也不调用 115 写接口。生产库存基线已完成，但真实隔离、清理执行和恢复验收仍未开启。
- REQ-010：新增只读库存派生层和 API：完整快照的新鲜度（`fresh`/`stale`/`incomplete`/`unknown`）、稳定 115 文件身份、可选摘要/infohash 精确重复、可选 TMDB 媒体/版本重复、仅文件名候选的 `needs_review` 检查，以及受 `review:write` 和 revision 保护的本地 TMDB 身份绑定；本轮补齐完整快照的 `removed` diff、`active`/`missing`/`restored` 对象账本和只读库存事件 API，并把库存门禁接入远端推送 worker：未配置库、库存不完整/过期/未知、精确重复或待确认重复都会在远端提交前失败；证据见 `REQ-010-library-inventory-deduplication/contracts/inventory-identity.md` 和 `evidence/2026-07-29-identity-freshness.md`。
- REQ-019：已完成第一阶段 Webhook：端点 Secret 加密、HTTPS/私网解析拦截、版本化脱敏 envelope、HMAC、持久 Outbox、指数重试、死信和管理员手动重试；真实接收端重放/篡改/重启验收和完整业务事件矩阵仍待外部夹具。
- REQ-020：已完成本地 MCP 工具阶段，并已完成当前 systemd 服务的受控远端 HTTP loopback 验收；MCP 层只排队现有计划操作，不直接执行 115 写入。远程 HTTPS、断线/重启恢复和完整兼容性验收仍阻断，证据见 `evidence/req-020-remote-agent-mcp-20260729.json`。
- REQ-021：已完成 PWA 安全基础：Manifest、应用壳、健康/任务/通知/workflow 只读网络优先缓存、版本失效、离线写保护、写请求幂等键、加密设备订阅登记和撤销，以及本站 Push 深链安全处理；真实 Push 投递、认证恢复、跨平台视觉和弱网验收仍阻断。
- REQ-022：尚未启动合集与系列管理；合集库存、补齐和订阅身份依赖 REQ-007、REQ-010 与 TMDB 合集契约，当前不能生成移动/复制源文件的合集计划。

## 阻断项

| 需求 | 状态 | 阻断原因 | 继续条件 |
|---|---|---|---|
| REQ-001 | 生产探针仍阻断，夹具写契约已通过 | 生产范围此前存在 uncertain 恢复结果，继续禁止生产写入；本轮仅在已核对为空的受管一次性目录完成双文件移动、重命名、恢复和精确回收，未触碰生产目录。证据见 `evidence/req-001-002-live-companion-20260730.json`。 | 由管理员先完成生产只读对账和范围配置，再用独立授权验收生产计划执行；在此之前只允许 fixture 验收和计划预览。 |
| REQ-002 | 夹具 full/incremental/cleanup 已验收，生产 STRM 阻断 | 整理完成事件会进入持久 dirty outbox，并由目录 generation 队列按“媒体库 ID + 稳定目录 ID”合并；worker 已支持同目录去重、`running -> dirty -> queued` 重入队、租约恢复和旧事件迁移回填，只有完整树快照后才触发增量对账。正式 API 夹具验收已覆盖 complete snapshot、稳定播放入口、重复生成幂等、改名增量、恢复、清理计划和 digest/confirm 清理执行，输出目录为临时目录。当前没有生产媒体库完整扫描、生产 STRM 清单、稳定播放入口和元数据联动，直链/Range/媒体服务器仍未纳入。 | 管理员配置并验证生产媒体库范围，完成一次完整只读扫描；随后单独冻结生产播放契约并在低风险本地输出目录完成 metadata/cleanup 证据，再评审生产开关。 |
| REQ-003 | 第二阶段部分完成，生产与外部验收阻断 | Agent API、Token/Scope、生命周期、审计和结构化错误基础已完成；`watchctl` 已覆盖只读查询、整理计划生成/执行、受保护的 STRM 全量/增量命令、清理计划和 digest/confirm 执行，以及基于同一 API 的只读 STRM 清单/文件内容校验；媒体库范围令牌隔离和 401/404 资源隐藏已验收。Web 高风险批准、完整任务关联、生产播放入口和跨平台端到端验收仍缺失，生产 STRM 继续保持门控。 | 补齐高风险 Web 批准和任务关联，再提供生产完整扫描、稳定播放入口及跨平台媒体服务器夹具完成外部验收；写命令继续复用 Scope、当前完整扫描、幂等键和 uncertain 保护，不建立 CLI 专用绕过路径。 |
| REQ-004 | 阶段完成，整体阻断 | 结构化日志、设置审计、隔离账本、整理操作和 STRM 清理事务内审计已具备；认证安全事件、推送任务生命周期日志和站内通知第一阶段已接入；本轮新增 Agent Token 创建、暂停、恢复、撤销、请求认证和 Scope 拒绝的结构化/持久审计，并补齐日志 API/设置页的等级、状态、事件码、请求/关联/任务、操作者和资源筛选（专项后端 `18 passed`、业务事件回归 `6 passed`、前端 `42 passed`，四视口 E2E `4 passed`）。媒体库配置/范围事件字段已补齐，新增事件目录契约扫描（15 个字面量调用、`5 passed`）。外部通知渠道、其他新增模块的完整事件矩阵和详情时间线仍未全部接入，当前不能宣称全业务审计完整。 | 建设外部渠道可靠 Outbox，逐项补齐新增模块事件矩阵、correlation ID 前端时间线、受限审计权限和覆盖率验收。 |
| REQ-012 | 第一阶段完成，外部渠道阻断 | 已复用持久通知账本实现 30 分钟去重、未读筛选、单条/全部已读，并接入任务结果通知；前端通知中心、偏好开关、任务/设置跳转及 API/组件离线测试已完成；本轮补齐 workflow 可行动阶段、任务 uncertain/失败、115 不可用、订阅新资源和备份失败的站内通知过滤，正常就绪/无新资源/普通日志不会产生通知；Webhook 增加单端点安全测试通知入口，禁用端点不排队；通知偏好新增默认 23:00-08:00 静默时段、IANA 时区和错误/安全事件突破静默，迁移与前端控件已通过专项回归。Bark/Telegram、渠道健康/重试、死信和完整事件矩阵仍未完成。 | 继续冻结通知事件模板和渠道 Secret 存储，完成真实外部接收端验收、渠道运营状态、重试/死信和完整事件矩阵后再开放更多外部渠道；外部渠道保持关闭不影响业务任务。 |
| REQ-005 | 阶段完成，召回验收阻断 | 部分缓存合并、TTL、结构化查询诊断和过滤原因码已通过离线测试；真实 PanSou 结构验收已有证据，但 95% 召回率仍需要至少 30 部电影、20 部剧集的脱敏对照样本，补充关键词持久策略和季度合集软评分也尚未冻结。 | 提供授权的脱敏对照样本和产品确认的补充关键词/软评分规则，完成相同查询计划下的 PanSou 对照、准确率与过滤原因验收。 |
| REQ-006 | 阶段完成，PanSou 样本基线阻断 | 检测核心已支持负缓存绕过、已有 torrent 只读复用、错误分类和批次统计；qB `v5.2.3`、登录、连接、356 个 DHT 节点、DHT/PEX/LSD readiness 已核对。6 个受控 Ubuntu torrent 在 120 秒最终等待内全部 verified 且清理完成，1 个旧版本负样本超时并清理完成；但当前 PanSou 抽取的 5 个样本仍全部 `metadata_timeout`，不能用受控样本替代 PanSou 召回基线。证据见 `evidence/req-005-006-controlled-qb-20260729.json`。 | 冻结并提供至少 30 部电影、20 部剧集的脱敏 PanSou 对照样本，完成 95% 召回、补充关键词/软评分、qB 真实样本成功率和来源分布验收；qB 受控链路可继续作为环境回归。 |
| REQ-007 | 第二阶段完成，自动动作/日历阻断 | 已实现默认关闭的订阅到期调度器：按 `next_check_at` 检查 active/matched/no_match 订阅，单项失败隔离，暂停/取消/完成跳过，调度只调用搜索检查且不创建 115 任务；配置为 `SUBSCRIPTION_SCHEDULER_ENABLED=false`。本轮完成 SUB-003 第一阶段资源观察账本、canonical key 去重、首次发现结果和观察列表 API，证据见 `REQ-007-subscriptions-auto-follow/evidence/2026-07-29-resource-observation-dedup.md`。确认/自动推送审批、全局 needs_auth 暂停、库存/检测联动、日历延期差异、剧集完结和真实上游验收仍未完成。 | 冻结 REQ-008、REQ-009、REQ-012 的自动动作契约；完成受控上游样本、确认/拒绝/自动推送幂等、库存去重、日历和完结规则验收后再开放自动模式。 |
| REQ-008 | 阶段完成，自动动作阻断 | 质量策略、可解释模拟、`global < library < media < subscription < temporary` 继承、版本快照和严格优胜模拟已完成并上线（迁移 023、027）；本批补齐订阅创建首次解析传入 `subscription_id`，订阅级绑定现在会正确写入策略版本快照。自动升级实际任务、库存/隔离/洗版联动仍保持仅提示，当前模拟不会创建 115 任务。 | 冻结策略与库存/整理联动契约，完成自动升级审批/幂等、needs_auth 暂停和隔离回滚验收后再开放自动动作。 |
| REQ-009 | 第一阶段完成，整体阻断 | 后端 workflow 持久化、统一 correlation ID、七阶段模型和分页接口已有；详情页首次检测/推送会创建或复用 workflow，检测批次和推送任务带 `workflow_id`，push 阶段会在任务创建、worker 终态、重试和重启恢复时同步；本轮补充服务端 `subscription_id`、`stage`、`stage_status` 筛选，受保护的人工确认和 fail-closed 取消接口，以及 `watchctl workflow approve|reject|cancel` CLI 关联；整理 operation 现在支持通过受保护 API 关联 workflow，并在排队、claim、成功、失败、不确定、取消和重试时同步 `organization` 阶段；STRM API 的全量/增量/清理请求和目录 dirty worker 现在可关联并同步 `strm` 阶段；可行动的 workflow 阶段事件现在会在提交后生成站内通知，并继续进入 Webhook Outbox；取消 workflow 时会停止未开始阶段，运行中或 `uncertain` 子任务保持原状态，不伪造远端撤回；运行中整理 operation 已增加持久取消请求和写入前后边界判定。STRM 子任务、通知完整事件矩阵和跨任务关联仍未完成。 | 补齐各子系统本地可中止契约、跨任务关联和通知完整矩阵，完成可靠渠道验收后再关闭整体阻断。 |
| REQ-010 | 第二阶段部分完成，增量/整理联动阻断 | 已完成生产目标完整递归扫描：54 页、12 个目录、43 个文件、54 个唯一对象身份；本轮将结构化 HTTP 405 只读回退接入固定 transport，新增受保护媒体库范围配置和只读范围验证 API，契约见 `evidence/req-010-configuration-gate-20260729.md`。生产数据库仍未通过 API 配置 `media_libraries`，也未完成应用内完整新鲜扫描；真实目录级增量事件源、整理/隔离/恢复结果统一写入账本仍未完成。生产写入不确定时继续禁止断言不存在、删除或隔离。 | 管理员通过受保护 API 配置明确生产目标 CID，完成只读范围验证和应用内完整库存扫描；再接入受控目录增量事件，并将整理/隔离/恢复结果写入同一账本。 |
| REQ-011 | 第一阶段完成，库存/元数据联动阻断 | 已完成 `episode_completeness` 纯规则矩阵和季度完整度只读 API：接口接入 `SeasonMetadataService`、最新库存扫描和已确认媒体身份，覆盖单集、多版本、未确认文件以及无完整扫描时的 `unknown` 门禁；专项 API 测试 `4 passed`。自动补集、订阅联动、库存变化后的 worker 重算、通知、真实多季样本和移动端视觉验收仍未完成。 | 冻结 TMDB 季集/播出状态契约，接入 REQ-010 的增量库存事件和 REQ-007 订阅后再开放自动补集；库存不完整或过期时继续禁止生成缺集任务。 |
| REQ-018 | 阶段完成，联动与执行阻断 | 已完成纯规则只读问题清单、健康分/模块分数/趋势和可逆修复计划门禁；人工构造样本可发现孤儿清单、失效 STRM、缺字幕、错误命名和 uncertain 任务。当前仍没有 REQ-010 生产库存基线，直链抽样、内封字幕、剧集映射、严重问题通知和真实复检 worker，不能生成生产删除/移动/隔离计划。 | 先接入 REQ-010/011/014 的稳定证据快照和 REQ-002 直链抽样契约，再接 REQ-012 通知与定向复检；库存或上游证据不完整时继续禁止删除类动作。 |
| REQ-016 | 阶段完成，执行阻断 | 只读容量统计、分类统计、完整快照重复候选和安全清理计划门禁已完成；生产库存快照已可证明完整且对象身份唯一，但隔离账本、审批、执行和恢复校验尚未接入。 | 接入 REQ-001 文件身份/隔离账本和审批后再生成生产隔离/清理计划；库存不完整时继续禁止删除类动作。 |
| REQ-017 | 第二阶段完成，实际恢复阻断 | 已有 SQLite 在线一致备份、原子快照、SHA-256、迁移清单和保留策略；新增只读恢复预览，可验证摘要、SQLite 完整性、迁移差异并提示 Secret 需重新配置；本轮补齐 `backup.created`、`backup.failed` 和 `backup.restore_preview` 结构化事件。恢复仍不会覆盖当前数据库；停机排空、恢复前快照、兼容矩阵、失败回滚、恢复后任务/库存/STRM 校验和 Web 二次批准尚未完成。 | 冻结恢复停机/排空、备份格式版本、兼容矩阵和恢复确认权限；完成损坏摘要、恢复失败回滚及 Secret 不泄露验收后，才允许实现实际恢复。 |
| REQ-015 | 第一阶段完成，外部验证/联动阻断 | 已新增 `/imports/preview` 和确认导入 API；仅接受合法 magnet 或配置域名的 115 分享，拒绝任意 URL/端口/用户信息，确认前不写库，确认后只保存加密资源且不创建任务。115 分享实时可用性、REQ-006 检测、REQ-008 质量筛选、REQ-009 workflow 和批量限流尚未完成。 | 冻结 PanSou/115 分享校验契约和导入批量限额，接入检测、质量、库存检查与 workflow 后再开放自动联动。 |
| REQ-024 | 第一阶段完成，升级执行阶段阻断 | 只读诊断已能核对应用版本、运行时、SQLite 完整性、迁移清单及外部组件脱敏状态；但升级源、签名/摘要、前后端/CLI/MCP 兼容矩阵和维护期间任务排空契约尚未冻结，无法安全实现升级、迁移覆盖或回退。 | 冻结可信发布元数据格式、目标版本兼容矩阵、升级批准 Scope、维护模式状态机和恢复检查点演练；在此之前仅允许读取诊断和本地备份。 |
| REQ-014 | 第一阶段完成，媒体/云端联动阻断 | 字幕分析接口只处理受限文件名，不读取正文、不接收路径、不访问外部字幕源；内封轨道信息需要真实媒体探测，STRM 同步需要 REQ-002 播放/清理契约，视频移动后的跨版本关联需要 REQ-001/010 稳定库存身份。 | 提供受控媒体样本和内封轨道工具契约，冻结字幕与视频版本、STRM 同步和外部来源权限后，再实现正文不落盘的轨道检测和同步计划。 |
| REQ-029 | 第一阶段完成，完整业务联动阻断 | 季度详情已独立于资源搜索缓存并支持语言回退、stale 标记和旧响应丢弃；但完整度矩阵依赖 REQ-010/011 稳定库存，订阅日历依赖 REQ-007，动漫集序依赖 REQ-025，真实多季资料和移动端视觉验收尚未完成。 | 冻结季度详情与库存/订阅/动漫映射的统一身份，提供受控多季样本并完成桌面/360px 移动端验收；在此前只开放季度资料读取。 |
| REQ-023 | 阶段完成，第二来源阻断 | PanSou 已接入统一来源适配器，但尚未提供经过授权、稳定契约和可控夹具的第二真实来源；不能用 fake 来源冒充生产召回证据。 | 提供第二来源 ID、协议/版本、访问授权和测试夹具，完成双来源健康/熔断、跨来源去重和真实验收。 |
| REQ-019 | 阶段实现，真实投递验收阻断 | 已完成 HTTPS/DNS 私网拦截、端点独立加密 Secret、版本化事件 envelope、HMAC 签名、持久 Outbox、指数重试、死信状态和管理员手动重试；端点列表新增由成功/失败历史派生的 `health_status`，DNS SSRF 及重试状态回归通过。当前没有受控 HTTPS 接收端证书/地址，真实接收端篡改/重放/重启恢复和完整事件矩阵尚未验收，证据见 `evidence/req-019-021-external-fixtures-blocked-20260729.md`。 | 提供受控 HTTPS 接收端完成签名/重放/重启恢复验收，并补齐完整事件矩阵后再开放自动发送。 |
| REQ-020 | 第二阶段部分完成，HTTPS/恢复/兼容性阻断 | 已实现只读 MCP HTTP JSON-RPC 与 stdio 适配、allow-list Resources/Tools、Scope/媒体库二次校验、任务/通知/工作流/整理计划分页、整理操作查询、digest/confirm/幂等排队工具、结构化结果/错误、调用审计和每身份调用限流；本轮修复 stdio 非对象输入、HTTP 错误和非对象响应导致的会话崩溃/非结构化输出，证据见 `evidence/req-020-stdio-contract-20260729.json`。当前 release 已完成受控远端 HTTP loopback Agent/MCP 验收，MCP 写工具复用本地 operation service，未绕过业务执行器。Tailscale Serve 被 tailnet 策略阻止，远程 HTTPS、断线/重启恢复和完整兼容性验收仍缺失，证据见 `evidence/req-020-https-blocked-20260729.md`。 | 由 tailnet 管理员启用 Tailscale Serve 或提供受控反向代理证书和地址，再演练断线与服务重启后的任务/Token 语义，完成 CLI/MCP 兼容性矩阵和真实服务验收；真实 115 写入继续由独立 transport/worker 门控。 |
| REQ-021 | 第二阶段部分完成，Push/跨平台验收阻断 | 已实现 Manifest/静态应用壳、健康/任务/通知/workflow 只读网络优先缓存、版本化失效、离线写保护、写请求幂等键和 PWA 设备订阅加密存储/撤销/字段校验，并加入本站 Push 深链安全处理；当前没有 VAPID 配置和真实 Push 设备，Push 实际投递、认证恢复以及 Android/iOS/桌面核心流程和弱网验收仍缺失，证据见 `evidence/req-019-021-external-fixtures-blocked-20260729.md`。 | 提供受控 Push 服务、VAPID 密钥和 Android/iOS/桌面验收设备，完成登录失效后的目标页面恢复、缓存时间展示、三平台和弱网验收；离线继续禁止写操作。 |
| REQ-022 | 阻断 | 合集与系列管理需要 REQ-007 订阅身份、REQ-010 库存身份和 TMDB 合集/系列响应契约；当前无法安全判断缺项或生成与库存一致的视图。 | 提供版本化 TMDB 合集夹具，冻结作品/合集/自定义系列身份和库存完整度，再实现只读合集视图与订阅关联；默认不移动或复制源文件。 |
| REQ-027 | 阶段完成，仍有覆盖阻断 | 后端结构化错误目录、安全兜底、字段错误和静态漏码契约已完成；前端目录现已登记后端全部 158 个错误码，并新增跨前后端漏码契约测试（后端 `5 passed`，前端错误/API 回归 `29 passed`）。表单/任务/通知动作、PWA 实际 Push 和完整 CLI/MCP 工具矩阵仍未完成。 | 补齐剩余 API 动态错误分支和所有用户操作入口的结构化结果/动作；提供受控 Push 与跨平台验收环境后再关闭完整覆盖阻断。 |
| REQ-028 | 阶段完成，外部集成阻断 | 独立资源搜索任务/快照、分页、持久化、取消/超时语义和性能遥测已完成；前端 E2E `149 passed`（含桌面/移动端 A/B、深链、失败恢复），前端单测 `99 passed`，Ruff 通过，生产构建通过。后端离线单元/契约 `590 passed, 3 skipped`；全量/集成命令在当前环境因外部服务等待超过 120 秒，未取得完整集成通过证据。 | 为外部依赖提供可控夹具或明确超时门禁后重跑集成套件；在此之前保持真实写操作关闭。 |
| REQ-025 | 第一阶段完成，库存联动阻断 | 已完成无外部依赖的 `anime_mapping` 领域模型和 5 项离线回归测试：支持标准季集、绝对集数、确认季度偏移、显式映射优先、重叠/越界复核，以及 OVA/OAD/SP/NCOP/NCED 等特别内容分类；尚无 REQ-010/011 的稳定库存与季集基线，也未接入作品身份、外部元数据、人工锁定 API 或整理/缺集/字幕联动。 | 先完成 REQ-010/011 身份基线，冻结 TMDB/人工映射字段和逐集证据模型，再以脱敏动漫样本实现预览、冲突和人工锁定；确认前不得移动或重命名文件。 |
| REQ-026 | 第一阶段完成，本轮不纳入真实探测 | 已完成 `media_technical` 纯领域模型和 5 项离线回归测试；按用户要求本轮不处理直链、Range 或媒体服务器，未获取或存储任何直链，也未调用媒体探测工具。 | 后续若纳入，先冻结直链不出库契约、Range/预算/隔离探测方案并提供受控媒体样本；L2 通过不得描述为完整文件无损。 |
| REQ-030 | 阻断 | 多维分类依赖 TMDB Discover/Genre 版本目录以及 REQ-001、REQ-007、REQ-010、REQ-022 的分类和本地状态索引；当前前端仍只有单 Genre/年份筛选，尚无稳定五大分区目录、保存视图或库存完整度语义。 | 先冻结浏览分类与 115 归档分类边界、版本化目录和 Discover 参数契约，再实现多选 Genre、地区/状态/评分筛选及 URL/分页恢复；库存不完整时不得输出确定的“未拥有”。 |
| C06/S08 | 本轮不纳入 | 按用户要求本轮不处理直链/媒体服务器；当前没有真实媒体服务器兼容验收证据。 | 后续若重新纳入，提供至少一个可控媒体服务器实例和版本，完成扫描、HEAD/GET/Range、拖动、续播、并发及敏感信息扫描后再评审播放门禁。 |

## 当前环境阻断

- 本地验证必须确认 `sys.path` 指向当前工作树 `/Users/apple/Desktop/115ts`；当前工作区尚未包含 `.venv`，不得使用系统 Python 或另一工作树的已安装包替代。准备当前工作区虚拟环境后使用 `./.venv/bin/python`，避免源码和测试错位。
- 全量 `pytest -q` 在 184 秒外部服务等待后超时；不依赖外部服务的 unit/contracts 已通过 `636 passed, 3 skipped`，前端 Vitest 已通过 `101 passed`，因此全量集成不能作为本轮完整验收证据。
- 生产数据库当前没有启用的 `media_libraries` 范围；REQ-010 推送门禁因此按 fail-closed 策略阻止远端提交，不能用测试目录冒充生产媒体库。继续条件是由管理员明确配置生产目标 CID，完成一次完整且新鲜的只读扫描后再恢复推送能力。

## 安全边界

- 本记录不写入 Cookie、Token、磁力链接、pickcode、完整播放 token 或真实直链。
- 未完成真实契约和人工确认前，不执行生产目录移动、重命名、删除或 STRM 清理。
