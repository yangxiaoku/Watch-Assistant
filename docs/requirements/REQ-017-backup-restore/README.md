# REQ-017：配置、规则与数据库备份恢复

| 字段 | 内容 |
|---|---|
| 版本 | V1.0 |
| 状态 | 待验收 |
| 优先级 | P1 |
| 依赖 | SQLite、配置系统、各需求数据模型 |

## 1. 一句话定义

为数据库、规则、受管清单和非敏感配置提供一致备份、校验、预览恢复和灾难恢复能力。

## 2. 目标与非目标

目标：

- 定时创建 SQLite 一致快照。
- 备份整理规则、质量策略、订阅、STRM 清单和任务关联。
- 恢复前检查版本、完整性和影响。
- 支持单项配置导出导入和整库灾难恢复。

非目标：

- 默认不把 Cookie、密码、Token 和加密密钥打包进普通备份。
- 不把备份文件上传未知第三方。
- 不在服务运行中直接覆盖数据库文件。

## 3. 功能范围

| 编号 | 功能 | 优先级 |
|---|---|---|
| BAK-001 | SQLite 在线一致备份 | Must |
| BAK-002 | 备份清单、摘要和版本 | Must |
| BAK-003 | 自动保留和容量限制 | Must |
| BAK-004 | 恢复兼容性检查 | Must |
| BAK-005 | 恢复预览和确认 | Must |
| BAK-006 | 配置与规则导入导出 | Must |
| BAK-007 | 恢复后校验 | Must |
| BAK-008 | 加密异地备份 | Should |

## 4. 备份内容

- SQLite 业务数据库。
- 应用版本、Schema 版本和迁移版本。
- 媒体库配置和规则版本。
- 订阅、质量策略、库存、STRM 清单和任务关联。
- 非敏感运行配置。
- 备份创建时间、文件大小和 SHA-256 摘要。

Secret 默认只记录“需要重新配置”的占位信息。若支持 Secret 备份，必须单独加密、显式开启并使用独立恢复密钥。

## 5. 恢复流程

1. 选择备份并验证摘要。
2. 检查应用、Schema 和配置兼容性。
3. 输出将新增、覆盖、忽略和需要重新配置的内容。
4. 停止领取新任务并等待安全点。
5. 创建恢复前快照。
6. 执行恢复和必要迁移。
7. 校验数据库、清单和任务一致性。
8. 恢复服务并生成中文报告。

失败时自动回滚到恢复前快照。

## 6. 界面与 CLI

- 设置页展示备份历史、大小、校验状态和保留时间。
- 支持立即备份、下载脱敏配置、恢复预览和删除旧备份。
- REQ-003 可执行只读列表和创建备份；恢复必须具备独立高风险 Scope 和 Web 批准。

## 7. 验收标准

- 在线备份在并发读写下保持数据库一致。
- 损坏或摘要不匹配的备份不可恢复。
- 不兼容版本明确拒绝或要求先迁移。
- 恢复预览不修改系统。
- 恢复失败可以回滚。
- 普通备份中不包含 Cookie、Token、密码和明文密钥。
- 恢复后订阅、任务关联、库存和 STRM 清单一致。

## 8. 分期建议

1. 一致备份、摘要和保留。
2. 配置导入导出和恢复预览。
3. 整库恢复、回滚和校验。
4. 加密异地备份。

## 9. 待确认项与建议默认值

| 决策项 | 默认值 |
|---|---|
| 自动备份 | 每日一次 |
| 本地保留 | 7 份 |
| Secret | 不进入普通备份 |
| 恢复 | Web 二次批准 |
| 恢复前快照 | 强制 |

## 10. 本轮实现证据（2026-07-29）

- 备份服务已记录 `backup.created`、`backup.failed` 和 `backup.restore_preview` 结构化事件；日志只包含状态、错误码和数量，不写入备份路径、数据库内容或 Secret。
- 备份与日志回归测试 `161 passed`，Ruff 通过。
- 实际停机恢复、恢复前快照、失败回滚、配置导入导出和 Web 二次批准仍按 `BLOCKERS.md` 保持关闭。
