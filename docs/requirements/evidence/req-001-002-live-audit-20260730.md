# REQ-001 / REQ-002 真实受管验收补充证据

更新时间：2026-07-30 16:40（Asia/Hong_Kong）
候选基线：`codex/publish-main@0a4a2e2`

## 计划确认

- 所有真实写入只使用独立 live runner，不绕过应用的范围、计划摘要、一次性授权和
  receipt-before-verify 门禁。
- 测试范围仅包含受管测试 CID 及其临时子目录；生产目标 CID 为独立配置，未在本轮
  真实写操作中触碰。
- 永久删除保持关闭。STRM 清理只在临时应用和受管 STRM 夹具中验证，生产清理保持关闭。

## 离线证据

- 组织设置/API、调度和操作回归：`13 passed`。
- P115 组织/库/播放/STRM 契约回归：`58 passed`。
- 前端全量 Vitest：`106 passed`；生产构建成功；Ruff 成功。
- 设置页覆盖“定时开关关闭仍可立即整理、停止整理、保存 revision”和六个可收起配置分组。

## 真实受管证据

- C02 只读复验：`success`、`complete=true`，读取 2 页；未执行写操作。
- 组织写入 runner：预览 `source_count=1`、`write_started=false`；确认后完成移动、重命名、
  恢复和临时目录回收，`receipt_count=6`、最终状态 `restored`。
- STRM 应用 runner：初始完整扫描和生成成功；远端改名后扫描检测到 1 个变化并完成增量生成；
  恢复后再次增量生成和清理成功；`production_root_touched=false`、`permanent_delete_used=false`。
- STRM 受管输出内容敏感字段扫描为 `clean`；不包含 Cookie、pickcode、完整 token 或时效直链。
- 生产目标最新递归只读扫描：54 页、11 个目录、43 个文件、54 个唯一对象，`complete=true`。

## 线上开关核验

重启前备份了 `/etc/watch-assistant.env`。当前健康接口确认：

- `organization_plan_enabled=true`
- `organization_execution_enabled=true`
- `organization_write_enabled=true`
- `organization_execution_supported=true`
- `strm_capabilities.full=true`
- `strm_capabilities.incremental=true`
- `strm_capabilities.cleanup=false`
- `strm_capabilities.playback=false`
- `permanent_delete_enabled=false`

真实写 worker 已具备领取已确认计划的能力，但生产自动整理仍必须先完成源目录/目标目录设置、
新鲜完整扫描和计划预览；没有确认计划时不会产生生产移动或重命名。

## 未关闭门禁

- 生产 STRM 首次建立、动态播放 HEAD/GET/Range 和媒体服务器兼容性未验收，播放保持关闭。
- 生产失效 STRM 清理和空目录清理未验收，清理保持关闭。
- 永久删除不可逆，保持关闭；后续只能在专用可丢弃夹具上另行审批和验收。
- 伴随文件联动、冲突洗版、隔离恢复和多对象失败恢复仍需独立计划与证据，不能以单文件
  runner 代替。
