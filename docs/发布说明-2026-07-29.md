# Watch Assistant 发布说明（2026-07-29）

## 发布范围

本次发布基于提交 `d5258c5`，包含已提交并完成离线验证的 Watch Assistant 基线、115
只读契约、媒体解析与匹配、分类命名、整理计划，以及本地整理操作状态和离线 transport。

本次不包含另一个工作树中的未提交改动，也不包含本地 Cookie、授权文件、部署截图、构建
产物或临时归档。

## 已验证

- 阶段后端全量测试：`794 passed, 4 skipped`（之后的执行器安全修复另跑定向测试）。
- 前端 Vitest：`78 passed`。
- 前端生产构建：通过。
- Playwright：`149 passed`。
- 组织操作与执行器定向测试：`120 passed`。
- 执行器部分写入安全修复定向测试：`29 passed`。
- 离线 115 整理 transport 契约：`39 passed`。
- Ruff、格式检查和 `git diff --check`：通过。

## 安全状态

所有真实整理、移动、重命名、STRM、播放、洗版、隔离和删除能力仍保持关闭。离线
transport 的 `live` 模式明确拒绝；它不读取 Cookie、不创建 `P115Client`、不访问网络，
也没有接入应用 worker、API 或生产配置。

TgtoDrive 没有稳定且已验证的提交/状态契约，真实推送继续禁用。未执行生产部署、生产数据
变更或真实影视目录操作。

## 下一步

先完成代码审查和发布分支合并，再单独安排真实 115 受管夹具验收。任何能力开关在对应
契约、计划预览、确认、幂等、审计和回滚证据齐全前不得开启。
