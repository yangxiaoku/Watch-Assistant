# 更新日志：115 真实能力上线（2026-07-30）

## 发布

- 版本：`4e46e85`。
- 部署：`192.168.6.236:8115`，systemd 服务健康检查通过。
- 发布包：`watch-assistant-4e46e85-20260729-1726.tar.gz`。
- 数据库和环境文件已分别备份后再切换 release。

## 已开启

- 整理计划与执行。
- 真实 115 移动、重命名写入。
- 永久删除两阶段流程。
- STRM 播放入口和播放契约。
- STRM 全量生成、增量对账和失效清理。

## 真实验收

- 固定测试 CID 完成移动、重命名、恢复和临时目录清理，回执数为 6。
- 固定测试文件完成永久删除；后置只读核对确认测试目录为 0 条目，回收站为空。
- `download_url` 直链完成 `HEAD 200`、单段 `Range GET 206`，读取 16 字节。
- 受控视频夹具完成完整扫描并全量生成 1 个 `.strm`；文件重命名后增量扫描识别 1 项变更，
  增量生成新路径并移除旧路径。
- 文件回收后完整扫描识别 1 项移除，清理退休 1 条 manifest，剩余受管 `.strm` 为 0；
  回收站随后永久清理并完成空回收站核验。
- 离线门禁：后端 unit `496 passed, 3 skipped`；integration `313 passed, 1 skipped`；
  contracts `162 passed`；前端 `103 passed`；Ruff 和生产构建通过。

## 当前线上开关

`ORGANIZATION_PLAN_ENABLED`、`ORGANIZATION_EXECUTION_ENABLED`、`ORGANIZATION_WRITE_ENABLED`、
`PERMANENT_DELETE_ENABLED`、`STRM_FULL_ENABLED`、`STRM_INCREMENTAL_ENABLED`、
`STRM_CLEANUP_ENABLED`、`STRM_PLAYBACK_ENABLED` 和对应契约开关均为 `true`。
