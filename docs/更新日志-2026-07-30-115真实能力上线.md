# 更新日志：115 受控计划基础上线（2026-07-30）

> 更正：此前文件将尚未完成真实验收的写入、删除和 STRM 播放能力描述为已开启。
> 本日志以 `02a359e` 发布后的线上状态为准。

## 发布

- 版本：`02a359e`。
- 部署：`192.168.6.236:8115`，systemd 服务健康检查通过。
- 发布包：`watch-assistant-02a359e-20260730.tar.gz`。
- 切换前已创建 SQLite 备份，并完成完整性检查。

## 已上线

- 整理计划、只读扫描和计划预览基础。
- 定时整理设置、立即整理排队和停止整理控制。
- STRM 全量/增量本地受管清单基础，仍受安全门禁约束。

## 已验证

- 固定测试 CID 完成移动、重命名、恢复和临时目录清理，回执数为 6。
- 组织 transport 的真实低风险写入证据仍限于专用 fixture，并已恢复原状态。
- 离线门禁：后端 unit `508 passed, 3 skipped`；integration `298 passed, 1 skipped`；
  contracts `162 passed`；前端 `105 passed`；Ruff 和生产构建通过。
- 发布包哈希已完成本地/远端一致性核对，数据库备份完整性检查返回 `ok`。

## 当前线上开关

`ORGANIZATION_PLAN_ENABLED=true`、`ORGANIZATION_EXECUTION_ENABLED=true`、
`ORGANIZATION_WRITE_ENABLED=false`、`PERMANENT_DELETE_ENABLED=false`、
`STRM_FULL_ENABLED=true`、`STRM_INCREMENTAL_ENABLED=true`、`STRM_CLEANUP_ENABLED=false`、
`STRM_PLAYBACK_ENABLED=false`。真实写入、永久删除、STRM 清理和播放在契约及真实验收完成前保持关闭。
