# 更新日志：115 整理计划路径修复与发布候选

发布日期：2026-07-30

## 本次更新

- 修复只读 115 网关不返回路径时的整理计划生成：扫描会根据已验证的父目录和名称重建相对路径。
- 对非法名称、路径穿越和异常目录关系继续失败关闭，不会生成可写入的整理计划。
- 新增真实应用级只读验收 runner，覆盖设置加载、范围验证、完整扫描和计划生成；低置信条目保持 `needs_review`，不会自动排队或写入。
- 保留“定时整理开关”和“立即整理”分离：定时关闭时不自动整理，手动立即整理仍可触发；停止整理会关闭并保存定时开关。

## 验收结果

- 低风险 115 夹具完成计划预览、移动、重命名、恢复和临时目录回收，回执数为 6，测试文件恢复原位置和原名称。
- 低风险 STRM 夹具完成全量、远端改名后的增量、恢复后的增量和清理；输出目录为临时目录，未触碰生产根目录。
- 永久删除 runner 返回 `write_gate_closed`，没有开始远端写入；永久删除继续关闭。
- 生产只读盘点完成且完整；生产数据库仍未配置影视库源目录和目标目录。
- 离线门禁：unit `513 passed, 3 skipped`；integration `307 passed, 1 skipped`；contracts `162 passed`；前端 `106 passed`；Ruff、Python 编译和 Vite production build 全部通过。

## 生产开关

- 115 整理写入开关和写入契约当前已生效；但生产数据库尚未配置源目录和目标目录，因此本轮没有生产整理写入。管理员仍需在网页设置中选择并保存目录后，才能进行生产计划预览。
- STRM 全量/增量能力保持开启，但生产 STRM 清理和播放继续关闭。
- 永久删除继续关闭，优先使用可恢复的隔离/回收流程。

## 发布信息

- 候选基线：`codex/publish-main` @ `a692c44`。
- 发布包必须在提交后从干净工作树生成，并使用包含最终 commit hash 的文件名。
