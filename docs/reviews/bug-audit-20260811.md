# Bug 审计报告（2026-08-11）

> 审查基线：`8954038`（codex/publish-main，2026-08-11 动态解析）
> 审查方式：6 路并行子代理分区全仓审查（core/security、api 层、adapters、组织/115 写路径、
> 其余服务、frontend）+ 死代码与文档专项核查。
> 本报告为**审计记录**；可执行的修复清单见同目录 `../fix-handoff-20260811.md`。

## 审查范围与基线

| 项 | 值 |
|---|---|
| 基线 commit | `8954038` |
| 后端 | 140 py 文件 / 67.8k 行（根层 13、api 31、adapters 18、services 78） |
| 前端 | ~42 源文件 / 11.2k 行 |
| 验证基线 | 单测 1093 过、集成 430 过、契约 299 过（含 B1 修复后）、vitest 232 过、ruff 干净 |

## 已在本轮修复（唯一例外，见 fix-handoff H0）

- **B1** `scripts/acceptance_closure.py::_native_library_path`：打包内置原生库路径被 Homebrew
  OpenSSL 遮蔽。修复：内置路径前置（独立提交 `62ccc13`）。该测试此前失败导致 verify.sh 门禁恒挂。

## 高危发现（P0/P1 级）

### P0 级风险（不可逆数据丢失）

| # | 位置 | 问题 | 失败场景 | 建议方向 |
|---|---|---|---|---|
| D0-1 | `services/p115_delete.py:129-149` + `adapters/p115_permanent_delete_transport.py:113-144,179-193` | 永久删除身份仅靠 `(parent_id, name, size_bytes)` 在"回收站快照后新增条目"中匹配，无原始 file_id 校验；`find_new_entries` 首帧出现即返回 | 并发动作把同目录+同名+同大小文件删进回收站 → 碰撞条目被误判为目标 → 对**非目标文件**执行不可逆 `recyclebin_clean` | 双观察窗口"持续唯一"才返回；≥2 候选即 UNCERTAIN；引入第二身份（大小+修改时间） |

### P1 高危

| # | 分区 | 位置 | 问题 | 建议方向 |
|---|---|---|---|---|
| H1 | core | `migrations.py:766-786`（070）+ `_subscription_indexes_exclude_cancelled`（未注册） | 070 唯一索引未排除已取消行；删重按 `MIN(id)` 保留，取消后重订场景会**保留已取消行、删掉活跃重订行**；带取消排除的迁移未注册进 MIGRATIONS | 注册迁移重建带 `status != 'cancelled'` 的索引；删重优先保留非 cancelled 行 |
| H2 | core | `migrations.py:1391-1415`（`_unique_scan_run_revision` 未注册） | 迁移 072 缺失，`uq_library_scan_run_revision` 唯一兜底对已存在库不生效 | 注册 `_unique_scan_run_revision` 为迁移 072 |
| H3 | api | `organization_operation.py:444-463` + `organization_plan.py:236-239` | retryable 服务不可用码（operation_unavailable/organization_plan_unavailable/reconciliation_unavailable/directory_ownership_unavailable）落默认 409 而非 503 | `_HTTP_STATUSES` 补 503 |
| H4 | api | `strm.py:383/229/938/997`、`strm.py:918-924/977-983/1088-1094/774-775/886-887`、`library.py:970-971`、`webhooks.py:131` | 大量非 catalog 错误码直接上抛（strm_operation_claim_conflict/lease_required/not_running/not_reconcilable/creation_conflict、strm_full_failed、target_*×15、webhook_endpoint_disabled、invalid_poll_interval 等）→ 前端无文案 | 补 _CATALOG 或归一到已定义码 |
| H5 | adapters | `p115.py:368-379` + `p115_library_gateway.py:78-82/186-193/558-564` | `target_cid=0`（115 根目录）合法配置但默认网关不传 `allow_virtual_root=True` → 根目录目标任务恒 UNCERTAIN，与 p115.py:1112-1115 注释矛盾 | 根目录目标传 `allow_virtual_root=True` |
| H6 | adapters | `qbittorrent.py:259-399/537-555/596-626` | 批次标记用每次新建 UUID，清理失败遗留无标记孤儿 torrent → 该 infohash 后续恒 ownership_conflict 且永不清理 | 固定前缀 category + 按前缀回收孤儿 |
| H7 | adapters | `prowlarr.py:374-416` | 仅解析前 20 个 download_url 结果，超限/失败被静默丢弃，不计 unsupported_count | 未解析结果保留为记录或计入 unsupported_count |
| H8 | adapters | `p115_playback_gateway.py:132-159` | `_get_transport` 的 `load()`/`transport_factory` 无超时包裹（与 library_gateway 不一致） | 按剩余 deadline 包裹 to_thread |
| H9 | adapters | `qbittorrent.py:114`、`tmdb.py:101`、`pansou.py:77` | 三处 httpx 默认 `trust_env=True`，环境代理可截获 qb 登录凭据 / tmdb api_key（prowlarr 已显式 False） | 明确 trust_env 策略 |
| H10 | services | `workflows.py:1099-1106` + `tasks.py:552-562/556` | 同一 workflow 并发推送第二个任务会覆盖 PUSH 阶段 child 绑定 → 第一个任务终态被静默丢弃（变 UNCERTAIN） | 创建前检查未终态 PUSH 子任务并拒绝 |
| H11 | services | `strm_operations.py:426-435` + `api/strm.py:758-785` | STRM 运行中操作无法取消（抛 `strm_operation_lease_lost` 误导），`is_cancelled` 对运行中恒 False → 长任务只能等完成 | 专用错误码；考虑取消请求位 |
| H12 | services | `organization_plan.py:907-910/668-670` | select_candidate/search 的 revision 更新无 CAS（读-改-写直接 commit）→ 并发选择静默覆盖；plan_hash 失配致整计划报废 | 改 CAS UPDATE（`WHERE revision=expected`） |
| H13 | services | `p115_delete.py` + `api/library.py:1066-1089` | P115DeleteService 服务层无写门禁评估；永久删除端点无审计/事件记录 | 服务层加 gate 参数；端点统一 audit |

## 中危发现（P2）

| 分区 | 位置 | 问题 |
|---|---|---|
| core | `security.py:602-611` + `api/strm.py:690` | `/strm-manifest`、`/strm-cleanup-plans` 未入 strm 标记集 → 仅 strm:read token 无法读清单（需双 scope） |
| core | `migrations.py:802-818`（071） | 只 DROP `get_indexes()` 可见索引；旧版表级 `UNIQUE(plan_id)` 为 auto-index 不可见 → 约束残留，终态后重试恒 409 |
| core | `security.py:632` | `/api/v1/organization-history` GET 落默认 `system:read`，含目标路径等敏感详情 |
| core | `app.py:1236-1304` | 后台 worker 全部 fire-and-forget 无监督/重启，启动失败 fail-open |
| core | `db.py:126-142` + `models.py:339-343` | 完成批次保留期内引用过期资源时 RESTRICT → 整笔清理回滚 |
| api | `settings.py:536-541` | `stop_organization` 未捕获 `SettingsConflict` → 并发 500 |
| api | `settings.py:596-657` | `export_logs` 无界循环拉全量日志 |
| api | `webhooks.py:132` | `webhook_dns_failed`（retryable）映射 422 |
| api | `settings_p115.py:310-315` | `device_not_found` 硬编码 409 应 404 |
| api | `organization_plan.py:109-112` | `plan_hash` 为 NULL 的历史计划提交 → `hmac.compare_digest` TypeError 500 |
| services | `inspection.py:324-337/482` | 非重启场景批处理可永久卡 RUNNING |
| services | `subscriptions.py:138-142/165-170` | COMPLETED 死终态（resume 可复活、无路径产生） |
| services | `strm_operations.py:750-766` | `_finish` 对 QUEUED 抛内部码泄漏客户端 |
| services | `organization_operations.py:1326-1368` | retry 不检查 active operation，IntegrityError 裸 500 |
| services | `organization_operations.py:1296-1307` | ORGANIZING 取消不校验 lease token |
| services | `organization_plan.py:518-521` | refresh 失效化非 CAS → revision 累加丢失 |
| services | `app.py:726` + `organization_directory_provisioner.py:70-73` | `lease_active` 为调用方传入布尔非 DB 验证，手动路径硬编码 True |
| services | `directory_dirty_worker.py:405-414` | run_forever 无异常隔离/退避，SQLite 锁错误杀死消费循环 |
| services | `library_index.py:161/805-809` | `_complete_lock` 实例级非按库；并发完成写同 revision → IntegrityError 误标 FAILED |
| services | `library_index.py:886-903` + `strm_scope.py:76-84` | 库 root 变更后旧 root 迟到 run 抢更高 revision → 新 root 快照判非最新，库操作全部 fail-closed |
| services | `empty_directory_cleanup_plan.py:194-215` | create_plan 按 plan_hash 去重后 commit 无 IntegrityError 捕获 → 并发双击 500 |
| services | `p115_login_devices.py:68-75` | add_device"清活跃+插入"非原子 → 双活跃设备 |
| services | `p115_permanent_delete_transport.py:74-111` | 回收站 10,000 条静默截断无完整性标记 → 永久删除恒 UNCERTAIN / SUCCESS 失真 |
| services | `directory_dirty_worker.py:300-301/351-353` | dirty 租约丢失不释放已 claim 的 STRM 操作 → 阻塞库操作最长 30 分钟 |
| services | `organization_operations.py:1239-1240` | recycle 动作不入历史，无法追溯 |
| frontend | `sw.js:47-51` + `api.ts` | 离线 API 回退返回 index.html 被当成功空响应 → 页面呈现"已登录但全空"掩盖真实故障 |
| frontend | `SettingsView.vue:368-396` | P115 二维码轮询卸载后仍写状态并续排定时器 |
| frontend | `SettingsView.vue:564/757/848/912` | save/load 异步回调无 settingsMounted 守卫 |

## 低危发现（P3）

- **core** `security.py:598-599`：strm-operations/{id}/cancel|resume POST 映射 strm:read（靠路由显式兜底）；`security.py:632`：watchlist/sources/reliability/resource-search 读落默认 system:read，resource-search 创建需 library:write、读结果却 system:read，scope 断裂；`config.py:63-65` script_token_hash 全局全权限；`config.py:75` cookie_secure 默认 False；`security.py:457-485` 限流只覆盖部分前缀；`cli.py:784` strm generate `--full` 死参数；`models.py:751-753` WorkflowEvidence 级联删除破坏证据链；`library_models.py:67-69` 库删除被扫描记录阻塞
- **api** `manual_import.py:38-39` resource_conflict 422 应 409；`pwa.py:51-55` pwa_device_conflict 422；`search.py:193` page 无上限；`search.py:198-199` 重复校验死代码
- **adapters** `prowlarr.py:418-440` downloadUrl 未校验路径前缀；`prowlarr.py:457-465` 兜底 GET 无响应体上限；`tmdb.py:466-478` Retry-After 无上限、page 无正整数校验；`tmdb.py:437-498` 无响应体上限；`p115.py:282-293` get_status 超时不丢弃 client、串行 101 页；`p115_library_gateway.py:39` MAX_SCOPE_VERIFICATION_PAGES 死常量；`prowlarr.py:230-233` 注入 client 改写共享 headers
- **services** 死代码：`media_classification.plan_batch`、`media_matcher.MatchStatus.AUTO_ACCEPTED`、`strm_operations._commit`；无界内存：`_CLAIM_LOCKS`/`_search_locks`/`_resource_search_tasks`/`_locks`；`notifications.py:172-204` 去重 TOCTOU；`webhooks.py:261-284` publish_due 无原子认领；`workflows.py:855-866` workflow_id NULL 去重失效
- **frontend** `App.vue:1061-1068` 超时后来源/缓存摘要不更新；`App.vue:1628-1629` 推送能力告警不对称；`polling.ts:30` pollUntil 默认 isDone 陷阱
- **core P3 已确认**：B2 App.vue loading 恒 false 死分支（清理项）；B3 深链 resource_page 用旧 totalPages 夹紧（App.vue:231）；B4 returnToBrowse 搜索页残留旧标题；B5 statusCatalog strmOperationStatusLabel 未查 skipped；**B6 反驳**：resourceSearchPolling 已返回 `timeout` 而非 `failed`，副作用已消除

## 错误码/事件码 triage（合并结论）

- **确属缺失、用户会看到通用文案**：`strm_operation_claim_conflict`、`strm_operation_creation_conflict`、
  `strm_operation_lease_required`、`strm_operation_not_reconcilable`、`strm_operation_not_running`、
  `invalid_cursor`、`invalid_limit`、`strm_full_failed`/`strm_incremental_failed`/`strm_cleanup_failed`、
  `webhook_endpoint_disabled`、`task_lease_active`、`device_unavailable`、`invalid_device_code`/`invalid_device_name`、
  `target_*`（约 15 个）、`invalid_small_file_threshold`/`target_file_invalid`、`invalid_poll_interval`、
  `invalid_conflict_mode`、`invalid_scan_interval_minutes`、`invalid_{schedule_enabled 等 15 个布尔字段码}`、
  `invalid_small_file_threshold_mb`/`invalid_operation_delay_seconds` —— **共约 27 个**
- **机器码/内部码（不入 _CATALOG）**：STRM manifest 安全/配置码（strm_manifest.py 内部捕获）、
  DeleteResult 响应字段码（p115_delete.py）、`source_health.reason_code`（prowlarr_*，非 HTTP detail）、
  任务详情 `task.error_code`（availability_* 等，非 HTTP 错误）
- 前端 errorCatalog 已覆盖后端 _CATALOG 全部 295 个码；前端多出的几个 strm 码与后端未注册码"超前镜像"一致

## 死代码审计

### 本轮已删除（独立提交，见清理分支 `codex/dead-code-cleanup-20260811`）

| 项 | 位置 | 证据 |
|---|---|---|
| 函数 | `services/media_matcher.py::confirm_selected_candidate` | 全仓（含测试）零引用 |
| 数据类 | `services/organization_plan.py::OrganizationPlanCandidate` | 零引用，不在 __all__；与 schemas/api 的 Response/Request 同名异类已区分 |
| 前端方法 ×6 | `api.ts::patchWorkflowStage/registerPwaDevice/pwaDevices/revokePwaDevice/organizationPlan/emptyDirectoryCleanupPlan` | 全仓零引用；连带清理未使用 import |

### 确认是"未接线的规划代码"，**不删**（对应活跃待验收需求）

| 模块 | 对应需求 | 判定 |
|---|---|---|
| `services/anime_mapping.py` | REQ-025 | 唯一实现基础 |
| `services/storage_governance.py` | REQ-016 | 唯一实现基础 |
| `services/library_health.py` | REQ-018 | 唯一实现基础 |
| `services/episode_coverage.py` | REQ-011 | 待核是否有替代实现 |
| `services/media_technical.py` | REQ-026 | 未接线纯函数 |
| `services/organization_isolation_contract.py` | 组织能力 | 疑被 companion_contract 取代，待确认 |

### 新发现的死代码（未删，入清单）

- `services/media_classification.py::plan_batch`（仅测试引用）
- `services/media_matcher.py::MatchStatus.AUTO_ACCEPTED`（值别名，零引用）
- `services/strm_operations.py::_commit`（零引用）
- `adapters/p115_library_gateway.py::MAX_SCOPE_VERIFICATION_PAGES`（导出未用）
- 前端仅测试引用的 5 方法（cleanupStrm/taskEvidence/workflowEvidence/strmCleanupPlan/searchSources）
- 前端 `types.ts::PwaDevice`（清理后无引用，未删类型定义）

## 文档审计与修复

本轮已修复（docs 分支 `codex/docs-audit-20260811`）：
1. AGENTS.md/PROGRESS.md/docs/ops：Windows 路径残留 → macOS/systemd 实际路径
2. README 部署章节：systemd 提为主流程，Compose 标注备用
3. README/PROGRESS/BLOCKERS/requirements 索引：过期 SHA 标注为 dated 历史快照
4. superpowers 设计稿：TgtoDrive 加弃用横幅

遗留文档缺口（未处理，按用户决策不补全新体系）：无独立 API 文档、无当前架构说明、无部署 runbook。

## 需要人工确认

1. P0 级 D0-1（永久删除身份碰撞）是否按 P0 排期优先修复
2. `POST /api/v1/search` 只要求 `library:read` 是否有意（会触发外部搜索与任务创建）
3. STRM"运行中操作不可取消"是否为本意
4. 同一 workflow 多 PUSH 子任务是否预期
5. 前端对任务详情 error_code 与健康 reason_code 是否需补文案
6. `organization_automation.py` 定时路径在 auto_execute 关闭时仍全量扫描是否有意
7. 迁移 071 旧约束形态（UniqueConstraint auto-index 可见性）需实测确认
