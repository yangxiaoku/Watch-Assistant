# 发布基线核对历史记录（2026-08-02）

## 2026-08-02 历史 Git 基线

- 在 2026-08-02 的核对时，`codex/publish-main` 与 `origin/codex/publish-main` 均指向
  `235ccc882297633ba3d19f09714d04ba09dc8e39`。
- 本记录只描述当日仓库发布基线，不代表该提交已经生成 release 包或部署到生产服务器。

## 当前发布核对规则

- 唯一发布分支为 `codex/publish-main`。发布前执行 `git fetch origin`，再用
  `git rev-parse origin/codex/publish-main` 获取实际最新基线；当前 commit 不在本历史文档中硬编码。
- 当前状态以 `PROGRESS.md`、需求索引和阻断记录为准；本历史记录不能推断当前生产版本、健康状态或线上开关。

## 当前发布边界

- 本次核对未生成 release 包、未执行部署，也未对 `192.168.6.236:8115` 做服务器只读核对。
- 历史 `docs/更新日志-*.md` 只记录各自日期的验证和部署事实，不能直接作为当前线上版本、健康
  状态或开关状态的证据。
- REQ-001/002 的受管夹具和离线阶段证据不等于生产影视库整理、生产 STRM 或稳定播放入口已上线。
- 真实 115 写入、永久删除、STRM 清理和播放继续服从独立功能开关、契约、计划/确认、幂等和审计
  门禁；未验收时必须 fail-closed。

## 基线内容摘要

- 当日基线中的多来源/Prowlarr 只读 readiness 仍需真实来源和生产验收，不把离线契约当作线上可用性；
  当前真实来源验证另受 Internet Archive 上游 timeout 阻断。
- release manifest、systemd release 权限/回退边界和独立 p115 runtime 的合入证据见
  [PR #14](https://github.com/yangxiaoku/Watch-Assistant/pull/14)、[PR #15](https://github.com/yangxiaoku/Watch-Assistant/pull/15)、
  [PR #16](https://github.com/yangxiaoku/Watch-Assistant/pull/16)、[PR #22](https://github.com/yangxiaoku/Watch-Assistant/pull/22)
  和 [PR #23](https://github.com/yangxiaoku/Watch-Assistant/pull/23)；对应测试见
  [`test_release_manifest.py`](../tests/unit/test_release_manifest.py)、
  [`test_release_deployment_scripts.py`](../tests/unit/test_release_deployment_scripts.py) 和
  [`test_release_archive_smoke.py`](../tests/integration/test_release_archive_smoke.py)。这些只证明代码门禁，
  不证明当前已打包或已部署。

## 后续发布核对

发布前必须重新确认 `codex/publish-main` 的实际最新提交、发布包 manifest、服务器运行模式、实际
release 目录、数据目录、健康接口和高风险能力开关；任何未核对项都保持未验证，不以本记录推断。
