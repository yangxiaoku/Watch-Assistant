# 更新日志：115 整理设置与 STRM 播放修复（2026-07-30）

## 发布信息

- 提交：`58474b4`。
- 发布包：`watch-assistant-58474b4-20260730-1935.tar.gz`。
- 部署：`192.168.6.236:8115`，systemd 服务健康检查通过。
- 部署前 SQLite 备份：`watch-assistant-pre-58474b4.db`，完整性检查为 `ok`。

## 本次完成

- 修复 STRM 播放动态链接的 User-Agent 契约，真实低风险夹具通过 HEAD、Range GET 和越权拒绝验收。
- 整理预览统一读取整理设置；视频扩展名、小文件阈值、重命名、地区和年份分类生效。
- 元数据扩展名参与字幕、NFO、图片等伴随文件匹配，并随主文件进入同一受控移动计划。
- 启用儿童节目、演唱会分类的明确文件名信号识别。
- 定时整理首次运行等待完整扫描间隔；手动立即整理仍立即排队。
- 115 移动、重命名操作延时读取设置值，不再固定为 0.25 秒。
- 补充设置值校验、合同测试、真实播放验收脚本和回归测试。

## 当前线上开关

- `ORGANIZATION_PLAN_ENABLED=true`
- `ORGANIZATION_EXECUTION_ENABLED=true`
- `ORGANIZATION_WRITE_ENABLED=true`
- `STRM_FULL_ENABLED=true`
- `STRM_INCREMENTAL_ENABLED=true`
- `STRM_PLAYBACK_ENABLED=true`
- `STRM_PLAYBACK_CONTRACT_VERIFIED=true`
- `STRM_CLEANUP_ENABLED=false`
- `PERMANENT_DELETE_ENABLED=false`

整理执行仅处理已确认计划和受管范围；永久删除继续关闭，STRM 失效清理继续关闭。

## 验证结果

- 后端 unit：`517 passed, 3 skipped`。
- 后端 contract：`165 passed`。
- integration：按文件拆分全部通过。
- 前端 Vitest：`106 passed`。
- Ruff 和 Vite production build 通过。
- 线上健康接口返回 `status=ok`，网页入口和主要前端路由返回 HTTP 200。

