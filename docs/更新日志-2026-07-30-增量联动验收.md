# 更新日志：115 增量联动验收（2026-07-30）

> 历史验收记录：以下部署、夹具和开关描述只适用于 2026-07-30；当前生产版本和开关必须
> 在发布前重新核对，本记录不代表 REQ-001/REQ-002 已完成。

## 代码版本

- 提交：`e21ed59`。
- 状态：已提交、推送并部署到 `192.168.6.236:8115`。
- 发布包：`watch-assistant-0bb0815-20260730-0655.tar.gz`。
- 部署前 SQLite 在线备份：`watch-assistant-pre-0bb0815-20260730.db`，完整性检查为 `ok`。

## 该次发布完成范围

- 整理完成事件接入持久化 dirty outbox，支持租约、过期重领、指数退避和最大重试失败。
- dirty worker 在完整且可验证的目录快照后触发 STRM 增量对账；扫描不完整时不会进入清理。
- STRM 增量接口不再隐式退休失效 STRM，清理必须由显式开关控制。
- 事件目录补齐 `strm.dirty_consumed`，并增加单元、契约和 worker 回归测试。
- 受管一次性夹具完成双文件上传、移动、重命名、恢复和精确回收，证据：
  `evidence/req-001-002-live-companion-20260730.json`。

## 验证结果

- unit：`511 passed, 3 skipped`。
- contracts：`162 passed`。
- 前端 Vitest：`105 passed`。
- Ruff 和 Vite production build 通过。
- 绝大多数 integration 文件通过；`test_inspection_api.py`、`test_search_api.py` 因外部服务等待未在 30 秒单文件门限内结束。
- 线上 systemd 已切换到 `watch-assistant-0bb0815`，健康检查返回 `ok`。

## 尚未上线的能力

- 生产 115 自动整理和生产目录写入仍关闭。
- 永久删除、STRM 清理和 STRM 播放仍关闭。
- 当前 worker 的增量边界使用完整且已验证的树快照，目录级最小扫描仍待专项性能验收。
- 部署前仍需生产媒体库范围配置、完整扫描和线上备份/健康核对。

## 2026-07-30 补充验收

- 新增 `scripts/p115_strm_application_live_runner.py`，将受管 STRM 夹具验收固定为正式 API 流程。
- 临时 SQLite 应用完成范围配置、只读范围验证和完整扫描，扫描结果为 `complete=true`。
- STRM 全量生成重复调用返回 `unchanged=1`；内容只包含稳定的本地播放入口，不含 Cookie、pickcode 或时效直链。
- 远端测试视频完成一次受控改名，应用扫描识别 `changed_count=1`，增量生成新 `.strm`；随后用独立一次性授权恢复原名并再次增量同步。
- 显式清理接口在完整扫描后执行成功；本轮永久删除未调用，生产根目录未触碰。证据见 `evidence/req-002-strm-live-20260730.json`。

本次补充不代表生产影视库已开启。生产范围仍需管理员配置并完成首次完整扫描，生产写入、永久删除和播放继续由各自开关与契约门禁控制。
