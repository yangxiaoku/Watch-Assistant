# 分支治理审计（2026-07-29）

## 当前基线

- 唯一发布分支：`codex/publish-main`。
- 生产运行版本：`a2bdfc8`；发布分支当前文档提交：`8bfa5e7`。
- 本次清理只处理没有关联 worktree、没有未提交工作且已完成审计的本地分支。

## 已归档

25 条本地旧分支已删除，提交完整保存在 `archive/20260729/<branch-name>` 标签中，必要时可按标签恢复。归档对象包括旧的 P115 契约、媒体库、组织计划、前端工作台和搜索实现分支。

## 保留

- `master`：根工作树仍有未跟踪的健康评估和环境文件，暂不切换或合并。
- 关联 worktree 的分支：仍有未提交修改或生产验收资产，必须逐个确认后处理。
- detached worktree 依赖的分支：保留，避免影响其他任务工作区。
- `origin/codex/publish-20260729`：包含独立的旧发布说明提交 `05f22e7`，尚未与当前发布基线完全合并，暂不删除远端引用。

## 后续规则

新增功能先合入 `codex/publish-main`，再删除对应分支；有价值但暂不合入的工作必须登记到进度文档或显式归档标签，不再创建 `clean/current` 变体。
